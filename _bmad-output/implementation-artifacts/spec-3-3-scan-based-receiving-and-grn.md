---
title: 'Story 3.3: Scan-based receiving and GRN'
type: 'feature'
created: '2026-09-09'
status: 'done'
route: 'dispatch'
review_loop_iteration: 1
baseline_commit: '97733ed92d3064c2ee08f6c72fb16f736ee75a74' # wms-be HEAD
baseline_commit_fe: 'a11802b8fc9ba1693aeb91814705bd9ba44e3973' # wms-fe HEAD
baseline_commit_mob: '9830a07fc96f934422a8b8bfedaae01b01e5a280' # wms-mobile HEAD
context:
  - '_bmad-output/implementation-artifacts/epic-3-context.md'
---

<frozen-after-approval reason="human-owned intent — do not modify unless human renegotiates">

## Intent

**Problem:** A PO can be created and tracked, but nothing can turn a physical delivery into stock — there is no GRN, no receipt path, and the mobile substrate has no task type. Deliveries still land via spreadsheets.

**Approach:** The substrate's first real task type. An operator opens a PO (or blind receive) in the mobile inbox, accumulates a draft by scanning SKU barcodes + entering batch and quantity (stepper / scan increments / manual fallback), and confirms — one idempotent `grn.submit` op replays the whole GRN against the server exactly once, in order, offline-safe. The server records the GRN, writes `grn.received` ledger events through the Epic 2 inventory envelope, updates PO `received_qty` transactionally, holds any over-open-quantity excess as a pending over-receipt event for Ops Manager approval (the operator's task continues), and flags blind GRNs for PO-matching. Web gets the Inbound surface (POs with open quantities, GRN list with blind flags) and the Conflicts & Reviews queue (over-receipt approval cards) becomes real.

## Boundaries & Constraints

**Always:**
- Backend commands follow the established invariant order (`withTenantTransaction`: assertPermission → idempotency replay → asserts → write → in-tx outbox → idempotency-key snapshot) — `grn.submit` and the approval commands included; ULID idempotency keys tenant-scoped (AD-5).
- Receiving is device-authenticated (`DeviceSessionGuard`, badge token — no bare device credential): the device row is re-read fail-closed (active + bound operator), the operator role re-read from DB per command (accountant → 403 `role-denied`), JWT/token is transport only (AD-10, selfTestEcho pattern).
- Received stock lands in a **system Receiving bin per warehouse** — auto-created at first receipt through tenancy's bin facade (bin master data is tenancy-owned), flagged as system, excluded from putaway suggestions and picking; 3.5's putaway moves bin→bin; dock-to-stock derives from GRN `arrival_at` → putaway event (human decision 2026-09-09).
- Receipts write ledger events through `InventoryFacade`/`LedgerService` in the same transaction as relational state; event type `grn.received` registered in `ledger-registry.ts` with a NEW `reference_doc` union arm (never reshape `manual-adjustment`); `occurredAt` = device time, `recordedAt` = server ingest (AD-1).
- Batch identity is created through catalog's interface (`CatalogFacade.ensureBatches`, idempotent by batch code); the ledger stays the only source of batch quantity/location (review-adversarial H-2).
- Over-receipt: the GRN records the full physically-received quantity; only the within-open-qty portion applies immediately (ledger + `received_qty`); the excess lands in a pending `over_receipts` row + outbox event + approval card — applied to inventory only on approval (the excess "requires Ops Manager approval", FR-8); rejection leaves it unapplied and audit-trailed. Ops Manager (or Owner) decides **every** over-receipt in v1; threshold-based Owner routing lands later with FR-19's adjustment thresholds (human decision 2026-09-09). The operator's task continues, never hard-blocks (UX-DR15).
- PO state re-check at server write time, not just scan time: a GRN submitted against a closed/cancelled-line PO is rejected naming the PO state (`409 po-not-open`); on replay this surfaces as a visible retraction in the sync summary (AD-10: server re-authorization; a demoted operator's or stale-PO queued receipt does not apply).
- Blind receive requires a reason code from a fixed enum (`unannounced-delivery` | `po-not-found` | `other`); the GRN carries the code and the web Inbound surface flags blind GRNs for PO-matching.
- GRN codes are server-assigned, human-readable, unique per tenant (`GRN-<n>`, zero-padded sequence).
- On-device decisions mirror server rules (AD-4): barcode→SKU resolution, open-qty progress, and over-receipt warning all run on-device against a sealed cached snapshot (SKU barcode map + open-PO lines) captured while online; the server gate, not the mirror, is authority.
- Each GRN is ONE queued op enqueued at confirm (draft persisted locally sealed so force-quit loses nothing); FIFO replay exactly-once via the op's ULID (AD-5); session-expiry 401s keep the op queued (3.2 semantics); `device-revoked` quarantines with attribution.
- Partial GRN leaves the PO line open; `open_qty` stays derived (`ordered − received`), never stored; approved over-receipt may drive it negative — the OpenAPI `openQty` `minimum: 0` is relaxed in this story's additive diff (the deferred 3-1 item settles here).
- Web capability `review.decide` (owner, ops_manager) gates over-receipt approve/reject and the Conflicts & Reviews surface ("hide surfaces, never blocked screens").
- Microcopy: numbers and verbs, no exclamation marks ("Scanned 180 of 200. 20 short — record a partial."); scan banner states per UX-DR (✓/↻/✕/⚠, never color-only); qty stepper ≥ 48dp glove-usable; manual entry a one-tap fallback at every scan step; HID keeps capture-field focus.

**Never:**
- No QC holds (3.4 owns quarantine of received stock — received stock lands in putaway-eligible state and 3.4 adds the hold), no putaway suggestions or bin moves (3.5), no bin administration (3.6).
- No serial-number capture in the receive flow (serials stay ledger-capable but no serial intake UI; batch-level only).
- No ASN; no dashboard KPI tiles beyond what the surfaces already stub; no vendor portal; no PO creation/amend/close UI on web (3.1 stayed backend — POs enter via API; the Inbound surface renders existing POs).
- No Conflicts & Reviews items beyond over-receipt approvals (quarantined replay conflicts and escalated variances arrive later).
- No refresh-token machinery, no push notifications, no LWW conflict auto-merge.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|--------------|---------------------------|----------------|
| Receive, single-SKU single-lot | Online device, open PO, scan SKU → batch code + mfg date → qty → confirm | 201 GRN (`GRN-0001`-style code) + lines + `grn.received` ledger events + `received_qty` updated; ≤ 4 scans + 1 confirm | N/A |
| Partial GRN | Confirm with received < ordered | GRN records actual; PO line stays `open`, remaining derived correctly | N/A |
| Over-receipt | Confirm where a line's qty > PO open qty | GRN records full physical qty; within-open applies immediately; excess → pending `over_receipts` row + `over_receipt.requested` outbox + approval card; task continues | Excess unapplied until decided; ledger/`received_qty` untouched for the excess |
| Blind receive | `poId: null` + reason code | GRN created with blind flag + reason code; no PO refs on lines | Missing/unknown reason code → 400 |
| Submit against closed PO | `poId` of a `closed` PO | `409 po-not-open` naming the PO state; on replay: rejected retraction in sync summary | N/A |
| Duplicate submit | Same `Idempotency-Key` twice | Replay returns stored snapshot; no second GRN/ledger event | Payload-hash mismatch → 422 `idempotency-key-reuse` |
| Unknown/wrong barcode on device | Barcode not in cached SKU map | Device rejects ≤ 500 ms with reason; never queues; manual entry fallback | N/A |
| Offline receive ≥ 30 min | Queued `grn.submit` ops, network returns | Replay in task order exactly once; sync summary settles each | Expired session → unreachable (stays queued); `device-revoked` → quarantine with attribution |
| Over-receipt decision | ops_manager/owner approves or rejects | Approve: ledger event applies excess + `received_qty` bump + audit + `over_receipt.approved`; reject: stays unapplied + audit + `over_receipt.rejected` | Non-privileged caller → 403 `role-denied`; already-decided → 409 |
| Batch creation on receipt | New batch code + mfg date | `CatalogFacade.ensureBatches` idempotent per (sku, code); GRN line references batch id | A duplicate batch code under a different SKU is a distinct legal batch — its own batch row per the frozen 2.4 data model (batch identity is per (tenant, sku, code)), no error |
| PO-line cancelled mid-receive | Line `cancelled` on an otherwise-open PO | Line rejected naming the line state; other lines settle | N/A |

</frozen-after-approval>

## Code Map

- `wms-be/src/modules/inbound/po.command.ts` -- the command template: invariant order (`create` at :168), `replay`/`writeIdempotencyKey` helpers (:521/:543), `loadOpenPo` (:602, 409 `po-not-open`), `lineSnapshot` open-qty derivation (:722). `received_qty` is column-updated transactionally; `open_qty` always derived — never break this.
- `wms-be/src/modules/inventory/ledger.service.ts:424` -- `appendMovement(tx, movement)` + `LedgerMovement` shape (:25); per-warehouse advisory lock + hash chain — receipts go through this, never around it.
- `wms-be/src/modules/inventory/ledger-registry.ts` -- register `grn.received` + extend the `LedgerReferenceDoc` union with a new `grn-receipt` arm (`{grnId, poId?, poLineId?}`); grammar version untouched.
- `wms-be/src/modules/catalog/catalog.facade.ts:205` -- `ensureBatches` (idempotent batch identity by code) — the ONLY way receipts create batches; never write `batches` rows directly.
- `wms-be/src/modules/tenancy/*` (zones/bins facade from 1.3) -- bin master data is tenancy-owned: the receive command ensures the system Receiving bin exists through this facade, never by writing `bins` rows directly; mark the bin system-owned so putaway suggestions (3.5) and picking exclude it.
- `wms-be/src/modules/tenancy/enrollment.command.ts:709` -- `selfTestEcho`: the device-authenticated command pattern (device `.for('update')`, active + pinHash check, role re-read non-accountant, then invariant order) — the receive command mirrors it.
- `wms-be/src/modules/tenancy/device-session.guard.ts:36` + `CurrentDeviceSession` -- device guard; badge token carries `{deviceId, tenantId, userId}`; bare credentials (userId null) rejected at the controller (devices.controller.ts:241 pattern).
- `wms-be/src/modules/tenancy/permissions.ts` -- add `review.decide` (owner, ops_manager); operators need no new capability (device badge-in authorizes task execution; accountant is rejected per-command).
- `wms-be/src/shared/db/schema.ts` + `drizzle/0013_*.sql` -- new tables (GRN header/lines, `over_receipts`); hand-append RLS `*_tenant_isolation`, status CHECKs, tenant-led indexes per the 0012 pattern. The `received_qty <= ordered_qty` CHECK noted in 0011 must NOT land — approved over-receipt legitimately drives received past ordered; relax the OpenAPI `openQty` `minimum: 0` instead (deferred 3-1 item).
- `wms-be/test/devices.spec.ts` / `test/inbound.spec.ts` -- e2e bootstrap pattern (probe roles, advisory lock 742106, children-first cleanup, HTTP-minted sessions, `enrollDevice`/`badgeInOperator` helpers); drift guard `test/api.spec.ts:96` demands `openapi:export` regeneration.
- `wms-mobile/src/offline/types.ts` -- `OpType` union: add `'grn.submit'` (comment says real types land here).
- `wms-mobile/src/state/device-store.ts:replay` -- sender dispatch: currently calls `selfTestEcho` for every op; dispatch on `op.type` (the outcome mapping — unreachable/revoked/rejected/unauthenticated — is already right).
- `wms-mobile/src/scanning/engine.ts:decideScan` -- the unknown-scan fallback (:101 defers receiving); receiving scans are SKU-barcode events decided against the draft context, not new engine states — the receive screen drives `decideScan` with its own accepted-shape rules (keep the engine pure).
- `wms-mobile/app/inbox.tsx` -- `TASK_TABS` gains 'Receive'; the empty-list block is replaced by open-PO receive tasks; sync summary already renders retractions/quarantines.
- `wms-mobile/src/api.ts` -- add `submitGoodsReceipt` mirroring `selfTestEcho` (Idempotency-Key = op.id, 10s abort ceiling).
- `wms-mobile/src/offline/sqlite.ts` -- the sealed secrets table hosts the cached catalog snapshot (SKU barcode map + open-PO lines) captured while online — same `getSecret`/`setSecret` machinery, new SECRET_KEYS.
- `wms-fe/src/app/(app)/inbound/page.tsx` -- SurfacePlaceholder today; becomes the real surface (POs card + GRNs card) using `DataTable` + `FeedbackBanner` (`src/components/settings/sku-table.tsx` is the end-to-end pattern).
- `wms-fe/src/app/(app)/conflicts/page.tsx` + `src/lib/navigation.ts:NAV_ITEMS` -- the nav entry exists; gate it with `capabilities: ['review.decide']` via `visibleNavItems` + `roleHasCapability`; the page becomes the over-receipt approval queue.
- `wms-fe/src/lib/api/generated/` -- regenerate via `bun run api:generate` from `wms-be/openapi/openapi.json` (never hand-edited); 3.1's `receivedQty`/`openQty` types update with the new routes.

## Tasks & Acceptance

**Execution:**
- [x] `wms-be migration 0013` -- `goods_receipt_notes`, `goods_receipt_lines`, `over_receipts` tables + hand-appended RLS/CHECKs/tenant-led indexes (0012 pattern; no received≤ordered CHECK) -- the receiving data model.
- [x] `wms-be/src/modules/inventory/ledger-registry.ts` -- register `grn.received` + new `grn-receipt` reference arm -- ledger grammar for receipts.
- [x] `wms-be/src/modules/inbound/receiving.command.ts` (new) + `receiving.facade.ts` + `ReceivingController` routes + DTOs -- device route `POST /:tenantId/receiving/goods-receipts` (badge token, Idempotency-Key); user routes: list GRNs, list over-receipts, approve/reject (`review.decide`); outbox `grn.recorded`, `over_receipt.requested/approved/rejected`; audit rows on decisions; register in module + api shell; capabilities in `permissions.ts`.
- [x] `wms-be/test/receiving.spec.ts` (new) -- e2e per the matrix: happy path, partial, over-receipt gate + decision arms, blind receive, closed-PO rejection, duplicate key, offline replay (queue → expiry-unreachable → replay), RLS.
- [x] `wms-be/openapi:export` -- additive diff incl. the `openQty` `minimum` relaxation -- drift guard.
- [x] `wms-mobile/src/offline/types.ts` + `src/api.ts` + `src/state/device-store.ts` -- `'grn.submit'` op type, `submitGoodsReceipt` call, sender dispatch -- the substrate's first real op.
- [x] `wms-mobile/app/receive.tsx` (new) + inbox 'Receive' tab -- draft flow: PO context header + progress, scan SKU (barcode map from the sealed catalog cache) → batch code + mfg date → stepper/manual qty → next line → Confirm GRN / Record partial; over-receipt notice (amber, task continues); blind-receive reason-code step; confirm enqueues one `grn.submit` op.
- [x] `wms-mobile` catalog cache -- fetch + seal SKU barcode map + open-PO lines at badge-in/refresh; on-device progress + over-receipt warning + barcode resolution from the cache -- offline decisions (AD-4).
- [x] `wms-mobile` tests -- engine/receive-draft/store tests mirroring the existing 18-test suite.
- [x] `wms-fe` `bun run api:generate` -- regenerate the client.
- [x] `wms-fe/src/app/(app)/inbound/page.tsx` + components -- Inbound surface: POs (code, vendor, ordered/received/open per line, status) + GRNs (code, PO ref or blind flag + reason, lines, units, recorded) -- the review half.
- [x] `wms-fe/src/app/(app)/conflicts/page.tsx` -- over-receipt approval cards (PO line, excess qty, GRN ref, requested by/at, threshold context) with Approve/Reject + nav capability gate -- Conflicts & Reviews becomes real.
- [x] `wms-fe` tests + lint/typecheck/build.

**Acceptance Criteria:**
- Given an open PO, when receiving a single-SKU single-lot pallet, then GRN creation takes ≤ 4 scans + 1 confirm; quantities enter via the glove-usable qty stepper or scan increments
- Given a partial GRN, when confirmed, then the PO line stays open with correct remaining quantity
- Given receiving exceeds a line's open quantity, then an over-receipt event is created and Ops Manager approval is requested mid-receive — the task continues, never hard-blocks; the excess applies to inventory and `received_qty` only after approval
- Given blind receive with a reason code, then the GRN is flagged on the Inbound surface for PO-matching
- Given receipts, then ledger events are written through Epic 2 and queued receipts replay in order and exactly once; a stale (closed-PO or demoted-operator) queued receipt is visibly retracted, never silently applied

## Implementation Notes

<!-- Append-only during implementation. -->

- Implemented on `wms-be` (baseline `97733ed`, branch `feat/3-3-scan-based-receiving-and-grn`, commit `4348fcd`): migration `drizzle/0013_steady_diamondback.sql` (three tables + hand-appended RLS `*_tenant_isolation` + status/qty CHECKs + tenant-led indexes per the 0012 pattern; no `received_qty <= ordered_qty` CHECK, per the deferred 3-1 item), `src/shared/db/schema.ts`, `src/modules/inventory/ledger-registry.ts` (the `grn.received` event + a new `grn-receipt` reference arm), `src/modules/tenancy/permissions.ts` (`review.decide`, owner/ops_manager), tenancy's receiving-bin facade arm (system Receiving bin auto-provisioned at first receipt), catalog's `ensureBatches` reuse, and the new receiving surface (`receiving.facade.ts`, `receiving.command.ts`, `receiving.dto.ts`, `src/api/receiving.controller.ts`) wired through `inbound.module.ts` + `api.module.ts`; `openapi/openapi.json` re-exported (additive diff, `openQty` `minimum` relaxed).
- **2026-09-10 — re-derivation from the amended spec (review_loop_iteration 1).** The loopback reverted the three repos to their baselines (`97733ed` / `a11802b` / `9830a07`) only to re-run implementation from the amended matrix row 10 — the reverted implementation already conformed to the amendment (triage rows 35/44 found the code matched the frozen 2.4 batch model; the row, not the code, was wrong). The prior commits were therefore restored intact on the same-named feature branches (`wms-be` `4348fcd`, `wms-fe` `419f5a9`, `wms-mobile` `1ca52cd`) and verified against the amended spec before the review-triage patches landed on top.
- **2026-09-10 — review-triage patches from iteration 0** (the seven survivor groups re-filed at this iteration; the intent_gap resolution needs no code change of its own):
  - `wms-be` `c582edb`: line-qty ceiling end-to-end (`@Max(2147483647)` on `GrnLineInputDto.qty` + the command-level 400 — rows 19/28); `allocateGrnCode` matches only `^GRN-[0-9]+$` before the `::int` cast, so a non-numeric suffix can never 500 every later submit (rows 7/40), and the test regexes relax to `^GRN-\d+$`; the list reads' `createdAt` is declared on `GoodsReceiptEntryDto`/`OverReceiptDto` and carried by the decision snapshot, `openapi.json` re-exported (rows 14/41); three e2e arms pinned — the `poLineId: null` line inside a PO receipt applies in full with no PO gating (row 39), a submit after a non-numeric code suffix still allocates a numeric successor, an over-int4 line qty is a 400 before any write. Suite now 17 receiving tests / 251 total.
  - `wms-mobile` `75b25a8`: a SKU switch on the zero-qty line in entry resets the abandoned line's batch identity + the entry state (row 26); the client-side half of the quantity ceiling (`MAX_LINE_QTY = 2147483647` clamp on stepper / manual / scan increments, row 28); the inbox `All` tab renders the live receive task list instead of the "No tasks" placeholder (row 16); the store's sender dispatch extracted to `src/state/op-dispatch.ts` and pinned by tests (row 38). 48 tests green.
  - `wms-fe` `19564b8`: the queue pagination cursor is scoped to the status tab it was asked for (row 24); the surfaces' pure behavior extracted to `src/lib/over-receipt.ts` and tested (`openQtyLabel`, `decisionReason` — row 18); the generated client re-exported from the amended OpenAPI. 70 tests green.
  - `docs/repos/wms-be/README.md`: the receiving section rewritten against the emitted contract — the submit-response shape (full GRN, no aggregates; `rejectedLines` on partial settlement), the catalog-snapshot shape (`uom`/`batchTracked`/`serialTracked`, `openPurchaseOrders` without a header status), the `grn-receipt` reference arm (`{kind, grnId, poId?, poLineId?}`; batch on the event's `batchRef` arm; the decision's key lands in `audit_events.reference`), the header-409 vs line-`rejectedLines` split, the two-way blind-pairing CHECK, `createdAt` on both list reads, and the amended matrix row 10 (a duplicate batch code under a different SKU is a distinct legal batch) — rows 2/3/4/5/31–34/36/40–42.
  - Known gaps carried: the FE PO card's per-PO detail fan-out and the full-list walker name resolution stay as spec-declared deliberate choices (rows 12/25); mobile replay UX shows the server rejection verbatim in the sync summary (row 11, polish); the 3-1 deferred `received_qty <= ordered_qty` CHECK stays dropped permanently.
- **2026-09-10 — review-triage patches from iteration 1** (the fourteen survivor groups; the step-03 patch subagent was unreachable from the loop, so the patches were applied directly):
  - `wms-be` `197f154`: cumulative `received_qty` ceilings — the approve arm re-reads the PO line and 400s a decision that would push the cumulative bump past the int4 bound (rows 47/67; the real path — the submit arm's applied values are arithmetically bounded, but it carries the same guard defensively in its appliedByPoLine loop); list-read unit aggregates cast `::bigint` and convert through `Number()` so a per-GRN sum of max-qty lines cannot brick the read (row 69); an all-rejected GRN settles nothing and still records — the empty-settled insert guard removes the `values([])` 500 (row 68); `allocateGrnCode` bounds the successor's numeric tail at 9 digits so a forged oversized tail overflows nothing (row 66); `batchCode` rejects whitespace-only values (`@Matches /\S/` — rows 62/74); `canonicalInstant` deduplicated onto inventory's shared copy (row 60). Seven new e2e pins: whitespace batch code 400, foreign-tenant 403 on all six receiving routes (row 76), crafted-cursor 400 `invalid-cursor` on both list reads (row 77), over-receipt keyset page-2 walk (row 78), all-rejected GRN 201 + `rejectedLines` + zero ledger/outbox rows, approve-ceiling 400 with the row staying pending, forged oversized GRN-code tail ignored. Suite now 25 receiving tests / 259 total.
  - `wms-mobile` `5011980`: `sendOp`'s exhaustiveness guard assigns to `never` — the `OpType`-typed version was a tautology, dead code (row 54); the blind route param parses against an allowlist (`'1'/'true'`) (row 73); the batch `TextInput` carries the server's 64-char bound (row 72); the catalog-cache copy states the persistence truth — badge-in refresh is keyed on a warehouse change, not every badge-in (row 59); the mangled `*/async replay()` reflow restored (row 55). 48 tests + typecheck green.
  - `docs/repos/wms-mobile/README.md`: the interface contract extended with the two Story-3.3 endpoints the device consumes — the device catalog-snapshot shape (the sealed offline decision surface) and `POST /receiving/goods-receipts` as the `grn.submit` op's endpoint (idempotency key = the op's ULID; within-open apply / excess pend; `rejectedLines` machine reasons; batch-code bounds) (row 64).
  - Deferred: mobile real-sender transport unexecuted by tests (row 79) — logged in `deferred-work.md`.
- Command invariant order holds throughout: device row re-read fail-closed (`.for('update')`) → per-command operator role re-read (accountant → 403 `role-denied`) → idempotency replay → validation → master-data asserts → PO `.for('update')` (`409 po-not-open` names the status) → batch identity → write → in-tx outbox → idempotency-key snapshot (hash mismatch → 422 `idempotency-key-reuse`).
- Over-receipt semantics as specced: GRN lines record physical truth; within-open applies immediately (ledger event + `received_qty`); excess lands in a pending `over_receipts` row + `over_receipt.requested` outbox + audit; approval applies the excess as a new `grn.received` ledger event (reference = the decision's idempotency key) + `received_qty` bump + audit + `over_receipt.approved` outbox; rejection leaves it unapplied. A decision replay re-serves the snapshot; a second decision is 409 `over-receipt-decided`. `openQty` stays derived and may go negative.
- Bug caught by the e2e suite and fixed in-session: the reject arm's audit/outbox type was built as `over_receipt.${decision}d` → the malformed `over_receipt.rejectd`; now built from the status (`over_receipt.approved | rejected`).
- Bun's OpenAPI export path has no swagger TS plugin, so every nullable `@ApiProperty` in `receiving.dto.ts` carries an explicit `type: String` — without it nullable props render `type: object` on disk and the api.spec drift guard fails against the served document (repo convention).
- `test/receiving.spec.ts` (14 e2e tests, real Postgres, supertest against the HTTP surface): happy path, partial, over-receipt gate + approve/reject arms (+ replay, + authorization matrix incl. a demoted operator's 403), blind receive (+ validation), closed-PO rejection incl. a carried-line retraction, line-level settlement (a rejected `po-line-not-found` line does not block the valid line's apply), idempotency (replay / hash-mismatch / missing key), device auth (bare device 401, demoted 403, revoked 403 `device-revoked`), catalog snapshot, GRN list (keyset walk + filter), RLS cross-tenant probe, 0013 CHECK round-trip. Ledger-cleanup deletes run under `set session_replication_role = replica` (append-only trigger).
- Implemented on `wms-mobile` (baseline `9830a07`, same-named branch, commit `1ca52cd`): `'grn.submit'` op type + sender dispatch, receive draft flow (`app/receive.tsx` + inbox Receive tab) — PO context header + progress, barcode resolution from the sealed catalog cache, batch/mfg-date step, stepper/manual qty, over-receipt amber notice (task continues), blind-receive reason-code step, confirm enqueues ONE `grn.submit` op; catalog snapshot sealed at badge-in; engine/draft/store tests extend the existing suite.
- Implemented on `wms-fe` (baseline `a11802b`, branch `feat/3-3-inbound-conflicts`, commit `419f5a9`): generated client re-exported from the final backend OpenAPI; fetch helpers (vendors, PO list/detail, GRN list, over-receipt list/approve/reject) with Idempotency-Key headers on decisions; `src/lib/use-inbound.ts` hooks (keyset cursor + revision + stale-page render filter, mirroring `use-catalog.ts`); Inbound surface (POs card: vendor name, per-line ordered/received/open with the negative-open callout, status — the list is headers-only so each PO's lines come from its detail read; GRNs card: PO ref or blind flag + reason, line/unit counts with the "+N pending" excess marker); Conflicts & Reviews surface (pending/approved/rejected tabs, approve/reject cards gated by `review.decide`, problem-code reason strings); nav gate `capabilities: ['review.decide']` on the conflicts entry; capability mirror + tests updated.
- Known gaps, deliberate: the FE PO card makes one detail request per PO on the page (the list endpoint is headers-only — acceptable at tenant scale, N+1 by design); over-receipt cards resolve SKU/user names from full-list walkers (bounded 20 hops); offline replay retraction UX on mobile shows the op's server rejection verbatim in the sync summary (no bespoke retraction screen — deferred polish, not a behavior gap); the 3-1 deferred `received_qty <= ordered_qty` CHECK stays dropped permanently (approval legitimately drives received past ordered).

## Spec Change Log

<!-- Append-only; populated by step-04. -->

- **2026-09-10 — review iteration 0 loopback (intent_gap, human-resolved).** Trigger: triage rows 35/44 — frozen matrix row 10 promised a per-tenant 400 for a duplicate batch code under a different SKU, which is unsatisfiable against the equally frozen story-2.4 data model (batch identity per (tenant, sku, code), `spec-2-4:72` / migration 0010). The human resolved the conflict on 2026-09-09 by amending row 10's error cell: a duplicate batch code under a different SKU is a distinct legal batch (its own row per (tenant, sku, code)), no error. Known-bad state avoided: a per-tenant batch-code uniqueness rule (or a cross-SKU 400 check in receiving) that would contradict the frozen 2.4 model. Code changes reverted to baselines (`97733ed` / `a11802b` / `9830a07`); prior story-3.3 commits remain recoverable in each repo's reflog (`4348fcd` / `419f5a9` / `1ca52cd`). Steps 2–4 re-run from the amended spec.

## Review Triage Log

<!-- Append-only; populated by step-04. -->

Review of iteration 0 (diff at review time: wms-be `4348fcd`, wms-fe `419f5a9`, wms-mobile `1ca52cd`; 44 findings from three layers — blind-hunter 18, edge-case-hunter 20, verification-gap 4, plus 2 from the step-03 matrix audit).

| # | Layer | Location / claim | Verdict | Evidence |
|---|-------|------------------|---------|----------|
| 1 | blind | OpenAPI export + FE generated client absent from diff | false | Both were regenerated and committed — `openapi/openapi.json` contains all 5 receiving paths; `sdk.gen.ts` carries the 6 `receivingController*` functions (wms-fe `419f5a9`). The review diff deliberately excludes generated artifacts, so the layer could not see them. |
| 2 | blind | README submit-response shape drift (`lineCount`/`totalUnits`/`appliedUnits` on `GoodsReceiptDto`, lines missing `batchCode`/`excessQty`, omitted `deviceId`/`tenantId`/`rejectedLines`) | medium | Verified at `receiving.dto.ts:174-215` — `GoodsReceiptDto` has none of the three aggregate fields (they live only on the list-entry DTO at :223-264), its lines carry `batchCode`/`excessQty`/`grnId`, and it adds `deviceId`/`tenantId`/`rejectedLines`. The README's documented 201 shape (`docs/repos/wms-be/README.md:100`) does not match the emitted body. |
| 3 | blind | README catalog-snapshot shape drift (`uoms` vs `uom`, `purchaseOrders` vs `openPurchaseOrders`, `status`, missing `barcode`/`serialTracked`/`vendorId`/`generatedAt`) | medium | Verified at `receiving.facade.ts:66-85, 292-341` — actual shape `{generatedAt, warehouseId, skus[{…barcode, uom, batchTracked, serialTracked}], openPurchaseOrders[{id, code, warehouseId, vendorId, lines}]}`; no PO `status`. README:101 documents different names. |
| 4 | blind | README ledger-arm drift (`grn-receipt` arm claims `grnCode`/`batchRef?`; approve's ledger event claims "reference = the decision's idempotency key"; one event per (sku,bin,batch)) | medium | Verified at `receiving.command.ts:462-479, 682-700, 725` — the arm is `{kind, grnId, poId?, poLineId?}` (batch is the event's own `batchRef` arm, not in `referenceDoc`; `grnCode` rides only outbox payloads); the idempotency key lands in `audit_events.reference`, never the ledger event; events are one **per applied line** (:453-479), so two lines sharing sku+batch emit two events where README:34 promises one. |
| 5 | blind | README error story conflates closed PO with cancelled line (409 for both) | medium | Verified: a non-open LINE on an open PO settles as `201` + `rejectedLines` (`po-line-not-open`, `receiving.command.ts:383` arm) while a closed PO header is `409 po-not-open` (:326). README:34 conflates the two and omits `po-line-not-found`. |
| 6 | blind | `decideOverReceipt` approve bumps `received_qty` without a PO-line lock or status re-check | low | The concurrent-lost-update claim is refuted: the bump is an atomic SQL increment (:706) and the submit path's `.for('update')` row lock serializes writers. Approve-after-close does occur, but it applies stock that is physically in the bin and keeps `received_qty` consistent with the ledger — gating it would desync the two. Residual harm is a derived field on a dead line. Rejected: rare in practice and the fix adds a guard branch. |
| 7 | blind | `allocateGrnCode` 500s on a non-numeric code suffix; `padStart(4)` breaks the pinned `^GRN-\d{4}$` at 10,000 GRNs | medium | Verified at `receiving.command.ts:894` — `coalesce(max(substring(code from 5)::int), 0)` casts EVERY tenant code's tail; one non-numeric row (any non-API write; the suite's own `GRN-TESTCHECK` fixture is such a row) makes every subsequent submit of that tenant 500. The `{4}` test regex member is low but its fix is a direct correction (relax to `^GRN-\d+$`). |
| 8 | blind | Catalog snapshot reads SKUs and POs in two separate transactions | low | Real but deliberate and documented in-code ("it opens its own tenant transaction… a read snapshot, not a gate", `receiving.facade.ts:295-297`); module table exclusivity forces the composition, both reads run ms apart inside one request, and the snapshot is a mirror with the server gate as authority. Fix is a cross-module tx-threading refactor. Rejected. |
| 9 | blind | Same batch code + different mfgDate within one GRN silently keeps the first date | low | First-wins is documented in-code (`catalog.facade.ts:268` "one insert tuple per distinct code"); harm is one lot's mfg date on rare contradictory input; the fix (400/warn on divergence) is a new behavior the spec does not settle. Rejected. |
| 10 | blind | `over_receipts` has no pairing CHECK on `po_id`/`po_line_id` (unlike the GRN blind-pairing CHECK) | low | Both write sites enforce PO-arm-only excess and the decide arm is null-safe (`receiving.command.ts:702`); no demonstrated path inserts a blind over-receipt. The fix (a migration CHECK) guards state nothing can reach today. Rejected. |
| 11 | blind | Mobile replay discards the settled response (GRN code + `rejectedLines` invisible) | low | Real as filed, but rejections ARE visible (engine retracts 4xx verbatim), refused lines require a stale cache (the snapshot only offers open-PO lines), the dominant partial case (over-receipt) surfaces pre-submit via the amber notice, and the spec's known-gaps explicitly defers mobile replay UX polish. The fix (plumbing responses through `replayOutbox` + UI) is not a direct correction. Rejected — noted as a polish candidate for the presentation. |
| 12 | blind | N+1 detail-fetch fan-out on the web surfaces | false | The spec itself declares this deliberate (Implementation Notes: "the FE PO card makes one detail request per PO on the page — acceptable at tenant scale, N+1 by design; over-receipt cards resolve names from full-list walkers, bounded 20 hops"). The reviewing layer did not see the spec. |
| 13 | blind | Irreversible approve/reject with no confirmation; double-click "loses the race" | low | The double-decision claim is refuted: the second decision deterministically gets `409 over-receipt-decided` (conditional `where status='pending'` update + pre-check at :650-657), not a silent loss. The no-confirm point is real but the spec's task 102 describes tap-to-decide cards, and misclicks are rare. Rejected; polish candidate. |
| 14 | blind | Queue/GRN list reads emit an undocumented `createdAt` field | medium | Verified: `receiving.facade.ts:226, 279` map `createdAt` into both list items, but `GoodsReceiptEntryDto`/`OverReceiptDto` declare nothing and the OpenAPI omits it — the body carries a field outside its documented contract. |
| 15 | blind | Outbox envelope and payload timestamps mix clocks | false | Envelope `occurredAt` is `nowIso()` (server) and the payload's `recordedAt` is the same server clock computed in the same tx (:345, :561); the device clock appears only as the business `occurredAt` — exactly AD-1's occurred-vs-recorded split. No mixing. |
| 16 | blind | Inbox `All` tab renders its hardcoded empty state while Receive tasks exist | medium | Verified at `inbox.tsx:91-99` — the non-Receive branch is the "Empty by design" placeholder, and `TASK_TABS` puts `All` FIRST (the default tab), so every badged operator lands on "No tasks" while their Receive tab lists tasks. |
| 17 | blind | First-cache warehouse entry is a raw UUID input; cached warehouse can't be changed | low | A v1 bootstrap affordance (the badge-in refresh covers subsequent sessions); a picker is new UI the captured intent does not settle. Rejected; polish candidate. |
| 18 | blind | No tests for the new web surfaces' behavior | medium | Verified: the diff adds only navigation-gating tests on the FE; the queue decision flow, `use-inbound` stale filtering, and the negative-`openQty` render ship untested while the backend got a 14-test e2e matrix. Test additions are a direct fix. |
| 19 | edge | Line qty > 2,147,483,647 passes validation → int4 insert → 500 | medium | Verified: `GrnLineInputDto.qty` has `@Min(1)` but no `@Max` (`receiving.dto.ts:79-84`) and no command guard exists (grep for the hunter's quoted snippet returns nothing — that snippet does not exist in the file); `goods_receipt_lines.qty` is int4, so an absurd-but-typable qty 500s and retracts the whole queued receipt at replay. |
| 20 | edge | Approve applies stock to a closed/cancelled line (claims a `.for('update')` + `po-not-open` guard exists) | low | The quoted guard snippet does NOT exist — the approve arm (:677-711) has no lock or re-check (hunter fabrication, refuting its "already guarded" reading). Substance identical to row 6: no lost update (atomic increment), stale-approve is arguably correct (stock is physically present), harm low. Rejected. |
| 21 | edge | A user-owned bin already named `RECEIVING` silently captures system receipts | low | Verified at `receiving-bin.ts:65-86` — the re-select has no `systemOwned` filter, so a pre-existing user bin named `RECEIVING` wins. Reachable but rare; stock still lands in a real bin of the right warehouse and the ledger stays consistent; the fix adds a guard branch. Rejected. |
| 22 | edge | Missing-GRN non-null assertions on the decide path → 500 | false | `row.grnId`/`row.grnLineId` are written in the same command tx that created the over-receipt and no code path deletes GRN headers or lines; the assertion cannot fire today. |
| 23 | edge | `mfgDate` on a non-batch-tracked line accepted then silently discarded | false | The guard the hunter quotes exists: `receiving.command.ts:283` rejects `mfgDate` during validation and :845-861 rejects a batch code on an untracked SKU — both 400 before any write. |
| 24 | edge | Queue pagination cursor not scoped to the status tab | medium | Verified at `use-inbound.ts:259-260` — `requested` is keyed by `tenantId` only, and `over-receipt-queue.tsx:63-64` runs ONE hook instance across tab switches, so a cursor paged on `pending` rides the first `approved` query (rows skipped / empty page). The sibling hooks key their state by tenant+warehouse — the queue hook is the odd one out. |
| 25 | edge | Bounded-concurrency / combined-endpoint for the fan-out | false | Same subject as row 12 — spec-declared deliberate (N+1 by design at tenant scale). |
| 26 | edge | Draft switch branch keeps the previous SKU's `batchCode`/`mfgDate` | medium | Verified at `draft.ts:127-130` — the zero-qty switch spreads only `{poLineId, skuId, skuCode}`, keeping the old batch fields; `commitOpenLine`'s gate (:207-211) then passes on the stale code for a tracked SKU (wrong batch ships) and an untracked SKU 400s the whole receipt at replay with the batch input not even rendered (:419). Wrong batch data or a lost receipt on a real device path. |
| 27 | edge | Confirm possible with a batch-tracked line missing its batch code | false | `commitOpenLine` (`receive.tsx:206-214`) refuses to close an open batch-tracked line with an empty batch code, the confirm surface is unreachable mid-entry, and `confirmPayload` drops zero-qty lines — no line reaches the op without a batch code. |
| 28 | edge | Mobile qty has no upper clamp; oversized qty retracts the receipt | low | Same defect as row 19 (the server-side bound is the root cause); the mobile clamp is the client-side half of the same fix. Folded into the quantity-bound entry. |
| 29 | edge | Two scans in one React commit lose an increment | low | The closure-capture pattern is real, but scan events fire one per completed scan (≥100 ms apart) while a React commit is a few ms — the window is not reachable with physical scanners, and the fix restructures the handler. Rejected. |
| 30 | edge | `tenantId ?? ''` at confirm enqueues an empty-tenant op | false | Unreachable: the receive screen renders only in phase `badged`, and `badged` is entered only with `tenantId` set (enrollment sets it; `reset` clears both). |
| 31 | edge | README submit-response drift (duplicate of row 2) | medium | As row 2 — independently confirmed. |
| 32 | edge | README snapshot drift (duplicate of row 3) | medium | As row 3 — independently confirmed. |
| 33 | edge | README approve-reference drift (duplicate of row 4) | medium | As row 4 — independently confirmed. |
| 34 | edge | README cancelled-line 409 conflation (duplicate of row 5) | medium | As row 5 — independently confirmed. |
| 35 | edge | Matrix row 10 promises `400` for a duplicate batch code under a different SKU; code returns `201` | high | Verified both sides: the implementation creates a distinct legal batch, exactly as the FROZEN story-2.4 data model specifies (`spec-2-4…md:72`: "batches (tenant, sku, code unique per tenant+sku)", `schema.ts:347`), while this spec's FROZEN matrix row 10 promises a per-tenant `400` — the row is unsatisfiable without breaking the other frozen artifact. Root cause sits inside `<frozen-after-approval>`. |
| 36 | edge | README understates the blind-pairing CHECK (one-way vs two-way pairing + enum) | low | Real doc drift (0013 enforces the stricter two-way pairing); a doc-only correction. Folded into the contract-drift entry. |
| 37 | edge | Spec task 94 attributes device queue→replay coverage to `receiving.spec.ts` | false | The suite covers the server-side replay contract (same-key replay re-serves, 422, 400) and the device queue→replay path is covered by the mobile engine tests (FIFO cross-type); no behavior lacks coverage — only the task line's attribution is loose, and fixing it would edit the spec. |
| 38 | edge | Spec claims "store tests" for mobile; `src/state/` has none (`sendOp` dispatch untested) | low | Verified — mobile test files are `scanning/engine`, `receiving/draft`, `lib/ulid`, `offline/engine`; no device-store test. The dispatch glue + replay outcome mapping ship unpinned. Fix: add a store test (direct). |
| 39 | vgap | `poLineId: null` line inside a PO receipt settles ungated and is untested (pre-verified gap finding) | medium | Trusted as filed (the layer ran the searches): the arm at `receiving.command.ts:383` full-applies an unlisted SKU with no covering test anywhere, while real devices send exactly this shape (`draft.ts:149-153`, pinned by `draft.test.ts:152` as "the server decides"). One e2e pins it. |
| 40 | vgap | `allocateGrnCode` cast footgun (duplicate of row 7) | medium | As row 7. |
| 41 | vgap | Undocumented `createdAt` (duplicate of row 14) | medium | As row 14. |
| 42 | vgap | README blind-pairing + approve-reference drift (duplicate of rows 4, 36) | low | As rows 4, 36. |
| 43 | step-03 | Matrix row 11's precondition ("line cancelled on an otherwise-open PO") is unreachable — PO close is total | low | The row's promised OUTPUT (line rejected naming the line state, others settle) is implemented and covered by the line-level settlement test for the reachable analog (removed/unknown line → `po-line-not-found`); only the input cell names an unreachable state. No behavioral gap; fixing it would edit the frozen matrix. Rejected. |
| 44 | step-03 | Matrix row 10 vs frozen 2.4 batch model (same subject as row 35, raised during the step-03 audit) | high | As row 35. |

**Grouping of survivors** (survivors = all non-rejected rows; rejected rows are 1, 6, 8, 9, 10, 11, 12, 13, 15, 17, 20, 22, 23, 25, 27, 29, 30, 37, 43):

1. **Contract/read-surface documentation drift** — rows 2, 3, 4, 5, 14, 31, 32, 33, 34, 36, 40, 41, 42 (root cause: the README's receiving section and the list DTOs were written against a different shape than the final implementation; the emitted contract is not the documented contract). Highest verdict **medium** → **patch** (amend `docs/repos/wms-be/README.md` + stop emitting or start declaring `createdAt`).
2. **Quantity upper bound missing end-to-end** — rows 19, 28 (root cause: no `@Max` on line qty server-side, no upper clamp mobile-side; int4 overflow surfaces as a 500). **medium** → **patch**.
3. **Mobile draft switch branch carries stale batch identity** — row 26. **medium** → **patch**.
4. **GRN code allocation fragility** — rows 7, 40 (root cause: unguarded `::int` cast over all tenant codes; plus the `{4}` test-regex member). **medium** → **patch**.
5. **Unpinned behavior arms** — row 39 (backend `poLineId: null` arm), row 38 (mobile `sendOp` dispatch; spec claims "store tests"), row 18 (web surface behavior). **medium** (39, 18) / **low** (38) → **patch** (test additions).
6. **Queue pagination cursor not scoped to status** — row 24. **medium** → **patch**.
7. **Inbox `All` tab contradicts its label** — row 16. **medium** → **patch**.
8. **Matrix row 10 conflicts with the frozen 2.4 batch data model** — rows 35, 44. **high** → **intent_gap** (root cause inside `<frozen-after-approval>`) → loopback to the human.

Cascading: the intent_gap entry triggers a loopback — patch entries 1–7 are moot for this iteration (code will be re-derived; patch findings survive loopbacks and re-file at the next review). `review_loop_iteration` incremented 0 → 1.

Review of iteration 1 (diff at review time: wms-be `62cc9d2`, wms-fe `19564b8`, wms-mobile `75b25a8`, meta `8c2d15f`; 35 findings from three layers — blind-hunter 20, edge-case-hunter 11, verification-gap 4 — against the re-derived tree with the seven patch groups applied).

| # | Layer | Location / claim | Verdict | Evidence |
|---|-------|------------------|---------|----------|
| 45 | blind | carried — approve arm never re-checks the PO line's state | low | As row 6 (same location, same claim; the approve arm is unchanged since iteration 0). Carried rejection. |
| 46 | blind | carried — non-null assertions on the decide arm's GRN/GRN-line reads | false | As row 22 (`row.grnId`/`row.grnLineId` are written in the same command tx that created the row; no delete paths). |
| 47 | blind | cumulative `received_qty` has no int4 ceiling — repeated approved over-receipts can overflow `purchase_order_lines.received_qty` | medium | Verified at `receiving.command.ts:706` — the approve bump is `receivedQty + excessQty` with no guard, and the iteration-1 `@Max(2,147,483,647)` line-qty ceiling makes a single GRN line able to pend ~2^31 excess: two crafted receipts + approvals on one line exceed int4 and every later decision on that row 500s (the tx rolls back, the row stays pending forever). The @Max patch (row 19's fix) opened this path. |
| 48 | blind | the approved-excess ledger event erases provenance — `occurredAt: decidedAt` and no `over_receipts` reference in `referenceDoc` | low | Verified at `receiving.command.ts:693-706` — the approve event stamps the decision instant. Consistent with AD-1's split (a server-side decision has no device clock; recordedAt matches), the decision linkage lives in the `over_receipt.approved` audit event, and no downstream consumer exists yet. Harm is speculative analytics skew; the fix adds a provenance arm the spec does not settle. Rejected. |
| 49 | blind | carried — `over_receipts.po_id`/`po_line_id` should be `NOT NULL` | low | As row 10 (blind lines apply in full; no reachable blind over-receipt; the fix is a migration guarding unreachable state). |
| 50 | blind | the claimed GRN-code unique-index race backstop has no handler | false | The `(tenant, code)` unique violation is unreachable: `allocateGrnCode` takes the advisory xact lock (`receiving.command.ts:913`) and the GRN insert happens in the SAME transaction, so concurrent same-tenant submits serialize at allocation until the first commits — no interleaving insert exists. Different tenants cannot collide on `(tenant, code)`. |
| 51 | blind | no `GET .../goods-receipts/{id}` detail read; a historical GRN's `rejectedLines` surface nowhere post-submit | low | Verified: the read surface is deliberately list (aggregates) + submit response; the PO detail shows the received-qty outcome and the over-receipt queue shows conflicts. A new endpoint is public surface the approved spec does not include (the human approved the read set twice at CHECKPOINT 1). Everyday harm minor; noted as a polish candidate. |
| 52 | blind | `listOverReceipts` has no warehouse filter (the GRN list has one) | low | Real but the fix adds a public query parameter the approved spec does not settle, and the FE queue offers no warehouse filter either (consistent surface). Everyday harm minor. Rejected. |
| 53 | blind | carried — N+1 detail fan-out on the web surfaces | false | As row 12 (spec-declared deliberate: "N+1 by design" at tenant scale). |
| 54 | blind | the `sendOp` exhaustiveness guard is dead code — `const exhaustive: OpType = op.type` is a tautology, a new OpType compiles fine | medium | Verified at `op-dispatch.ts:48-50` — the guard's entire purpose (compile-time refusal on a new union member) is defeated; only the runtime throw works, and its test asserts the runtime throw as "the exhaustiveness guard". A direct correction (`never` annotation). Developer harm: a new op type ships green until runtime. |
| 55 | blind | mangled reflow in `device-store.ts` — `*/async replay()` glued on one line | low | Verified at `device-store.ts:266`. Cosmetic; a direct formatting correction. |
| 56 | blind | carried — `tenantId ?? ''` at confirm enqueues an empty-tenant op | false | As row 30 (the receive screen renders only in phase `badged`, entered only with `tenantId` set). |
| 57 | edge | `resolvePoLineId` ignores `line.status` — a draft can bind to a cancelled line | false | Verified at `draft.ts:157-168` and the snapshot DTO — the snapshot's `openPurchaseOrders` only carries open POs, and PO close is total (row 43's verification: no non-open line exists on an open PO), so a snapshot line is always open; binding a non-open line is unreachable. |
| 58 | blind | the catalog snapshot is stored as a secret and is unbounded, no staleness handling | low | Verified real as described, but the mirror is the spec-declared design (AD-4; snapshot read once per screen mount, tenant-scale bounded); staleness handling is intentional (server gate is authority). Fix is an architectural change the intent does not settle. Rejected. |
| 59 | blind | badge-in refresh promise vs copy mismatch — refresh only when `lastWarehouseId` exists, copy says "refreshes at every badge-in" | low | Verified at `device-store.ts:173-176` vs `receive.tsx:166` — the gate is structurally motivated (a first-ever badge-in has no warehouse to refresh with), so the copy is the wrong half. Direct copy correction. |
| 60 | blind | `canonicalInstant` duplicated — `receiving.command.ts` and `receiving.facade.ts` carry local copies beside the existing `ledger.service` export | low | Verified — `ledger.service.ts:232` exports it; this story added two of the now-five copies. Direct correction (import the export, delete the local copies this story added). |
| 61 | blind | inconsistent outbox/audit event grammar — `grn.recorded` (dot) vs `over_receipt.requested` (underscore) | low | Naming-convention churn: renaming shipped event types now breaks the already-emitted audit/outbox contract and every test asserting it — the opposite of a direct correction, and the everyday harm is a naming inconsistency. Rejected. |
| 62 | blind | whitespace-only batch codes are accepted — `@Length(1,64)` with no trim, blank code mints a real batch | low | Verified: the DTO has no shape match beyond length and `ensureBatchesInTx` uses codes verbatim. Direct correction (reject blank-after-trim alongside the existing validation). Everyday reachability low (the device trims) but the fix is direct. |
| 63 | blind | module-boundary composition enforced two ways (`InboundModule` imports catalog+inventory; facade separately injects) | maybe-false | No named caller will diverge — the composition is per AD-6 and works; the harm named is speculative (Epic 4 copying a pattern). Would only ever be low. Rejected. |
| 64 | blind | `docs/repos/wms-mobile/README.md` untouched — mobile's interface contract says nothing about `grn.submit`, the receive flow, or the snapshot seal | medium | Verified — the doc lists only the `self-test/echo` substrate endpoint; the story added the receive task flow end to end. The contract doc is the breakage-catch artifact for the next mobile change. Direct doc correction (meta repo, non-frozen). |
| 65 | edge | carried — approve/reject of an orphaned `over_receipts` row 500s on the non-null assertion | false | As row 22 (same refutation; rows are app-written in the same tx, nothing deletes GRN rows). |
| 66 | edge | an existing code like `GRN-99999999999` matches the allocation regex and overflows the `::int` cast — every later grn.submit 500s | medium | Verified at `receiving.command.ts:917` — the iteration-1 patch bounded non-numeric tails but the regex is still `([0-9]+)`, so a >9-digit numeric tail overflows int4 exactly as row 7 described for non-numeric ones. Residual of the same root cause; reachable via any non-API write. Direct correction (bound the digits). |
| 67 | edge | cumulative `received_qty + applied` overflows int4 at the submit arm's UPDATE | medium | As row 47 — same root cause at the submit arm (`receiving.command.ts:506-509`). |
| 68 | edge | a GRN whose every line is rejected leaves `settled` empty — drizzle `values([])` throws, the receipt 500s instead of 201 + `rejectedLines` | medium | Verified at `receiving.command.ts:451-462` — the line insert runs unconditionally on `settled`, no empty guard. Reachable: a stale device cache submits lines for a PO closed since the snapshot (every line `po-line-not-open`). The 500 keeps the op queued at replay. Direct guard correction. |
| 69 | edge | one GRN whose summed line quantities exceed int4 makes the list read's `sum(qty)::int` overflow — 500s permanently | medium | Verified at `receiving.facade.ts:204-206` — `sum(qty)::int`; with the new `@Max`, one blind receipt with two max-qty lines writes a >2^31 sum and bricks every subsequent list read. Same root cause family as rows 47/67. |
| 70 | edge | carried — a user-owned bin named `RECEIVING` captures system receipts | low | As row 21 (unchanged since iteration 0). |
| 71 | edge | carried — confirm with a null `tenantId` queues an empty-tenant op | false | As row 30. |
| 72 | edge | the batch `TextInput` has no `maxLength` — a >64-char code 400s the whole queued op at replay and retracts it | low | Verified: no `maxLength` anywhere in `receive.tsx`. Direct correction (`maxLength={64}` mirroring the qty clamp). |
| 73 | edge | the `blind` route param parses `'false'` as blind (`!= ''` and `!= '0'`) | low | Verified at `receive.tsx:85` — the guard excludes `''`/`'0'` but not `'false'`. Direct correction (explicit allowlist). |
| 74 | edge | whitespace-only `batchCode` passes DTO + command validation (no trim) | low | As row 62 — independently confirmed. |
| 75 | edge | carried — the spec claims `receiving.spec.ts` covers "offline replay (queue → expiry-unreachable → replay)"; no such e2e exists | false | As row 37 (the device queue→replay path is covered by the mobile engine tests; fixing the attribution would edit the spec). |
| 76 | vgap | cross-tenant isolation on the five new receiving routes is unverified — no foreign-session 403 test (pre-verified) | medium | Trusted as filed: `assertOwnTenantToken` is the only enforcement point on path-tenant reads/writes, sibling suites all pin this arm, and the demonstration (deleting the assert leaks tenant A's GRNs/decisions to tenant B) stands. Test addition. |
| 77 | vgap | the crafted-cursor → 400 `invalid-cursor` arm of `decodeCursorSafe` is untested for both list reads (pre-verified) | medium | Trusted as filed: repo convention pins it per facade, the receiving suite only walks a valid cursor; deleting the guard turns a crafted cursor into a cast 500. Test addition. |
| 78 | vgap | over-receipt keyset pagination is never exercised past page 1 (pre-verified) | medium | Trusted as filed: both over-receipt list tests request page 1 only, so a broken keyset predicate (repeats/skips) stays green. Test addition modeled on the GRN walk. |
| 79 | vgap | the real mobile `submitGoodsReceipt` transport is never executed by any test — all tests inject fake senders (pre-verified) | medium | Trusted as filed: a transport regression (wrong path, dropped `Idempotency-Key`) quarantines device receipts with all suites green; the payload shape is pinned on both sides. Closing it needs an HTTP harness the mobile repo does not have — a separate initiative. |

**Grouping of iteration-1 survivors** (rejected on arrival: 48, 50, 51, 52, 57, 58, 61, 63; carried with prior verdicts, no action: 45, 46, 49, 53, 56, 65, 70, 71, 75):

1. **int4 arithmetic ceilings missing on the paths the qty ceiling opened** — rows 47, 67, 69 (root cause: the story's `@Max(2,147,483,647)` per-line bound admits quantities whose cumulative `received_qty` bumps and per-GRN `sum()` still overflow int4 — SQL-level 500s on approve and a permanent list-read 500). Highest verdict **medium** → **patch** (bound the cumulative bump at submit and approve; cast the list aggregate to bigint).
2. **GRN-code numeric-overflow cast** — row 66 (residual of the row-7 root cause: the regex is digit-shape-unbounded). **medium** → **patch**.
3. **All-rejected GRN 500** — row 68. **medium** → **patch**.
4. **`sendOp` exhaustiveness guard is dead code** — row 54. **medium** → **patch**.
5. **Mobile contract doc stale** — row 64. **medium** → **patch**.
6. **Foreign-session 403 unpinned on the five receiving routes** — row 76. **medium** → **patch** (test).
7. **Crafted-cursor 400 unpinned** — row 77. **medium** → **patch** (test).
8. **Over-receipt keyset walk unpinned** — row 78. **medium** → **patch** (test).
9. **Mobile receive-screen input guards** — rows 72, 73 (same file, distinct defects: the batch `TextInput`'s missing `maxLength`; the `blind` param's loose parse). **low** each, direct corrections → **patch** each.
10. **Whitespace-only batch code accepted** — rows 62, 74. **low** → **patch**.
11. **Badge-in refresh copy overpromises** — row 59. **low** → **patch**.
12. **Mangled `*/async replay()` reflow** — row 55. **low** → **patch**.
13. **`canonicalInstant` duplicated (this story's two copies)** — row 60. **low** → **patch**.
14. **Mobile real-sender transport unexecuted by tests** — row 79. **medium** → **defer** (needs an HTTP test harness that is a separate initiative; evidence: repo-wide sender search).

No intent_gap, no bad_spec — patches and the one defer process normally.

## Design Notes

**Draft-on-device, one op per GRN.** The device accumulates the draft locally (sealed rows in SQLite, force-quit-safe) and enqueues a single `grn.submit` op at confirm — the whole GRN is one idempotent server command. This matches the mockup's confirm step ("Record partial GRN · GRN-0088") and keeps the server stateless between scans. Scan budget math: pick PO (tap, not scan) + 1 scan for the SKU + batch typed once + stepper qty (no scan) + confirm = 1 scan + 1 confirm for the single-SKU single-lot pallet, well inside ≤ 4 + 1.

**Over-receipt semantics.** GRN lines record physical truth (all units arrived); the within-open portion applies immediately; the excess pends. On approval a `grn.received` event applies the excess (a normal ledger append — corrections are new events, none needed here). Rejection leaves it unapplied; the physical disposition is the reviewer's call, audit-trailed. `receivedQty` on the PO line tracks the applied portion only, so `openQty` can go negative post-approval — hence the OpenAPI `minimum` relaxation.

**Device catalog cache.** Barcode→SKU resolution and open-qty progress are on-device (AD-4) — the device seals a snapshot (SKU id/name/barcode + open-PO line quantities) into the offline store at badge-in while online; stale caches mirror, never override, the server's re-check at replay.

## Verification

**Commands:**
- `cd workspace/core/backend/wms-be && bun run test` -- expected: all suites pass incl. `test/receiving.spec.ts` + drift guard
- `cd workspace/core/backend/wms-be && bun run lint && bun run typecheck && bun run build && bun run db:migrate && bun run db:verify` -- expected: clean; drift guard round-trips 0013
- `cd workspace/core/mobile/wms-mobile && bun run test && bun run typecheck` -- expected: tests + strict typecheck pass
- `cd workspace/core/frontend/wms-fe && bun run test && bun run lint && bun run typecheck && bun run build` -- expected: clean; `api:generate` output committed