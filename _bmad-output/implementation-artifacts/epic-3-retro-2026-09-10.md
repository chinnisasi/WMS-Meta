---
epic: 3
date: 2026-09-10
verdict: accepted-with-open-items
criteria: declared
headless: false
---

# Epic 3 Retrospective — Receiving & Putaway: The Floor's First Flow

## Epic summary

**Epic:** Epic 3 — Receiving & Putaway (FR-7…FR-11, FR-29, FR-30; realizes UJ-1). Six stories, all `done` in sprint-status; `pending_stories` was empty at retro time.

**Stories completed:** 3-1 PO creation/lifecycle · 3-2 mobile client substrate · 3-3 scan-based receiving & GRN · 3-4 QC hold & release · 3-5 directed putaway · 3-6 bin administration.

**Evidence inventory:**

| Evidence | Available | Reference |
| --- | --- | --- |
| Epic spec + declared acceptance criteria | yes | `_bmad-output/planning-artifacts/epics.md` §Epic 3 (per-story AC blocks) |
| Story specs (6) | yes | `spec-3-1…spec-3-6-*.md` in `_bmad-output/implementation-artifacts/` (each with review triage log) |
| Sprint status | yes | `sprint-status.yaml` — 3-1…3-6 `done`, retro `optional` → `done` at finalize |
| BE diff range | yes | `d75f45f..33f57ea` (wms-be, 6 story commits; 3-1…3-6) |
| FE diff range | yes | `2c1f824..c2173d1` (wms-fe, 5 commits; 3-2…3-6) |
| Mobile diff range | yes | `d924697..e9e85a4` (wms-mobile, repo root → main; 6 non-merge + 3 merge commits; 3-2, 3-3, 3-5) |
| Meta docs | yes | spec/contract/triage PRs #14–#18 in WMS-Meta |
| Previous retro | yes | `epic-2-retro-2026-09-09.md` (follow-through checked in Phase 4) |
| Session logs | partial | `~/.claude/projects/-Users-sasidhar-Documents-WMS-Meta/*.jsonl` — full transcripts exist for the story sessions; used selectively for process lessons (no per-story automated extraction) |

**Change volume (non-merge):** wms-be ~13k added lines across 68 files (largest: receiving.command.ts 978, putaway.command.ts 935+929, bin.command.ts 559, putaway.facade.ts 368, migrations 0011–0016, five e2e suites ~5.5k); wms-fe ~4.7k added (use-inbound.ts 672, inbound-cards.tsx 561, devices-card.tsx 299, zone-bin-setup.tsx 203, generated client +2.2k); wms-mobile ~3.9k added across 25 files (receive.tsx 758, putaway.tsx 585, inbox.tsx 209, drafts + offline engine/tests). Commit→story attribution: the script matched only subjects with hyphenated story keys (3-3, 3-6); dotted subjects ("story 3.1", "3.4", "3.5") required manual mapping — recorded as a nit-level process note, not a finding.

## Findings

Sources: spec reconciliation over epics.md + all six story specs + deferred-work.md vs actual code in all three repos; aggregate views over wms-be (`d75f45f..33f57ea`), wms-fe (`2c1f824..c2173d1`), wms-mobile (root..main); diff-scope review lenses (adversarial / edge-case / verification-gap) on the combined 29.4k-line diff; behavior check above. Every finding below carries its source.

### Spec-to-build reconciliation (26 declared AC lines → 27 audited items)

**20 met · 4 met-with-recorded-deferral · 1 reinterpreted-with-record · 2 silently diverged.** No claimed-but-missing backend capability anywhere; every putaway/velocity/report gap is properly papered (spec Decided blocks + `deferred-work.md`). The two defect-class items both sit in Story 3.2's mobile block and both lack a deferral record:

- **F-1 — Story 3.2 claim/claim-lost UX never built and never deferred.** AC: "task claimed by another Operator disappears within one refresh cycle; claim-lost 'Claimed by {other} while offline' with queued-line disposition." Zero hits for claim machinery in wms-mobile or wms-be; tasks are derived and unclaimed by design (spec-3-5 Never: "No bin claim/assignment machinery — FR-29's claim UX arrives with picking"). But `epic-3-context.md:25` still restates the claim UX as an Epic 3 requirement, spec-3-2's frozen AC rewrite silently dropped the wording, and `deferred-work.md` has no entry. Currently vacuous (picking is Epic 4) — the defect is the missing record, not the missing code.
- **F-2 — Story 3.2 "nearest correct bin" rejection hint not built, not recorded.** Putaway wrong-bin rejection says "`{bin.code}` is blocked — pick another bin" (`src/putaway/draft.ts:156`); the suggested bin shows in the task header, never in the rejection. The frozen 3.2 matrix quietly dropped the phrase. Microcopy sibling riding the same AC: the ↻ chip word is "Queued", not the declared "Recorded · queued" (`theme.ts:84` vs epics AC + UX-DR17).
- **F-3 — The 1.5 s / 500 ms scan budgets are argued, never measured.** `src/scanning/engine.ts` is a synchronous pure decision (met by construction) but no on-device benchmark was ever run or recorded; the only AC in the epic whose "met" rests entirely on an architectural argument.
- **Recorded (for the record, not defects):** "carried" as-built = successor PO (human decision 2026-09-09); PO creation is API-only with no FE surface (per spec-3-1 Never); approved over-receipt drives `openQty` negative (3-1's `minimum: 0` pin relaxed in 3.3, recorded); blind flag lives on Inbound, not dashboard (recorded); encryption is field-level AES-256-GCM sealing, not SQLCipher (recorded with interop test).

### Aggregate views — wms-be

- **F-4 — The architecture spec's facade-guard test is structurally vacuous.** `test/architecture.spec.ts:118-128` detects cross-module inventory offenders by matching the literal string `modules/inventory/` in file source; every real consumer imports via the relative `../inventory/...` form, so the regex can never fire. Proof: `inbound/qc.command.ts:20`, `putaway.facade.ts:24`, `receiving.facade.ts:30`, `qc.facade.ts:12` import `canonicalInstant` — an inventory *internal* — and CI stays green.
- **F-5 — Direct cross-module table reads contradict the modules' own AD-6 docblocks.** `tenancy/bin.command.ts:5-13` imports and reads inventory `batches/batchOnHand/stockOnHand` + catalog `skus` directly at `:532-608`, `:805-840` while its DI comment (`:186-188`) asserts "this module never touches inventory tables directly (AD-6)". Same in `putaway.command.ts` (reads inbound + inventory + catalog tables), `receiving.command.ts:845-860` (reads `skus` directly while also using CatalogFacade), `putaway.facade.ts:269-285`. The enforced test covers only inventory *writes*.
- **F-6 — Facade seams bypassed by the new bin admin.** `bin.command.ts:28` imports `openQcHoldsForBinsInTx` from `inbound/qc.command.ts:80` (absent from `QcFacade`); `bin.command.ts:29-35` imports `binFull/binBlocked/binOccupancyInTx/binRetired*` from `putaway/putaway.command` rather than `PutawayFacade`. The seams exist; tenancy reaches into command-file internals.
- **F-7 — Idempotency scaffolding unfactored: the ~25-line `writeIdempotencyKey` exists 5× near-verbatim** (`putaway.command.ts:644`, `qc.command.ts:577`, `po.command.ts:529`, `receiving.command.ts:814`, `bin.command.ts:997` — diffed identical), the replay block appears in 15 command files, and the `idempotencyKeyReuse` error is homed in `tenancy/registration.command.ts:147` — an epic-1 story file imported by 14 other files across all five modules.
- **F-8 — Per-arm stock enumeration re-implemented 5+ ways** (facade `qcScopeOnHandInTx`/`serialsLocatedInBinInTx`/`qcHeldArmsInTx`, putaway `receivingBinOnHandInTx`/`binCandidatesInTx`, bin mergeBin's arm loop `bin.command.ts:532-608`, retireBin's empty gate `:805-840`); serial arms have two reading paths (facade vs `putaway.command.ts:834` reading `serials` directly). The `(tenant, warehouse, bin[, sku]) + gt(quantity,0)` where-clip is restated across 6 files.
- **F-9 — System-bin ensure helpers: two near-verbatim ~55-line copies in one file.** `tenancy/receiving-bin.ts:46` `ensureReceivingBinInTx` vs `:109` `ensureQcHoldBinInTx` (the QC copy is labeled "the Receiving-bin ensure's mirror"); a third system bin would copy it again.
- **F-10 — Bin-domain errors have two homes with conflicting semantics.** `tenancy/bin.errors.ts` (3.6's shared home; `binRetired409` = 409) vs `putaway.command.ts:871-920` (3.5's factories; `binRetired*` = 400 for the same code `bin-retired`). Story 3.6 re-homed the toggle but left the split. Bin-state residual coupling: `BinStateCommand` lives in putaway, imports its snapshot type from `tenancy/bin.command.ts:16` and errors from `tenancy/bin.errors.ts` — the bin domain's helpers cross both directions across the split.
- **F-11 — God-class caveat.** The epic-2 codebase was already this big (`ledger.service.ts` 1667, `reservation.service.ts` 948 at epic start; all six epic-3 command files are new files). But `bin.command.ts` (1022) is the first command file past 1000 lines, `putaway.command.ts` carries a single ~455-line method (`placePutaway :189-643`), and 5 of the top 8 module files are now epic-3 files.
- **F-12 — Cursor/uuid guard copies multiplied.** `decodeCursorSafe`+`invalidCursor` near-verbatim in `tenancy.service.ts:420`, `users.command.ts:618`, `enrollment.command.ts:811` (new, 3.2), `sku.command.ts:334`, `putaway.facade.ts:44` (new, 3.5 — diverges by one regex), plus inline variants — despite the shared `pagination.ts` primitive existing since AD-9. (Epic-2 retro a1's underlying ms-truncation also still present — see Previous-retro follow-through.)
- **Clean, measured:** naming (`*Command/*Dto/*Facade` across all six stories), error construction (179 `ProblemException` sites, zero command-layer Nest exceptions), migrations 0011–0016 (RLS policy on every new tenant table, text+CHECK status sets, pairing CHECKs, chain-citing comments — no drift), bin-row locking (single `binOccupancyInTx` helper, consistently `.for('update')`), no TS file-level cycles, Nest module graph acyclic (the tenancy docblock's "no back-edges" claim is true at Nest-module level, not TS-import level), ledger containment (+18 lines), ledger event types added as additive registry arms.

### Aggregate views — wms-fe + wms-mobile

- **F-13 — `zone-bin-setup.tsx` grew 681 → 884 in 3.6** and is now the largest component file in the repo (2.6× the next): 8 subcomponents plus the merge-picker with its own fetch-race sequencing (`mergeFetchSeq`, `:583-696`) inline. `use-inbound.ts` (672) spans 5 domains + 4 reference-map hooks. Mobile `receive.tsx` (757) / `putaway.tsx` (584) carry full wizard screens inline; the pre-epic screen ceiling was ~235. The pure decision logic *was* extracted and tested (`src/receiving/draft.ts`, `src/putaway/draft.ts`) — the residual is UI-only.
- **F-14 — The requested-cursor hook skeleton is copy-pasted ~9× in wms-fe** (`use-users.ts`, `use-catalog.ts`, `use-zone-bins.ts`, `use-devices.ts` — the last a near-verbatim diff of use-catalog, new in 3.2 — plus 5 copies inside `use-inbound.ts`), no `useCursorPage(fetcher, scope)` primitive. Contrast: full-chain fetch-all *did* converge on one `fetchAllPages` primitive (8 call sites, no new copies in epic 3).
- **F-15 — 7 un-pruned private `rejectionReason(error)` copies** in wms-fe (one per card; common cases re-translated in every copy) while stories 3.3/3.4 *did* extract theirs into a shared tested module (`src/lib/over-receipt.ts`) — the repo demonstrates both the converged and the diverging form within one epic.
- **F-16 — wms-mobile still has no generated client.** `src/api.ts` is a hand-written typed client (149 → 384 lines in the epic); every backend contract change must be manually mirrored (3.3 added `submitGoodsReceipt` + interfaces by hand; 3.5 added the whole `CatalogSnapshot` type tree by hand). Standing drift obligation carried from Epic 1 retro (still open).
- **F-17 — Mobile micro-duplications:** `PUTAWAY_MISMATCH_REASON_CODES` declared twice (api.ts:238 + putaway/draft.ts:23, the api.ts copy dead weight that can drift); blind-reason labels in the receive *screen* while mismatch labels live in the *draft module* (same concern, two homes); badge session parsed twice with bare `JSON.parse as BadgeSession` and no validation (`device-store.ts:99`, `:224`) while the catalog cache got a defensive parser.
- **F-18 — The FE hook gap: `use-inbound.ts` (672 ln) shipped with no test file**, nor `use-devices.ts` — the largest untested lib surface yet, including the pure map/enrichment logic that is the most testable code in the file. (Mobile is the mirror image and clean: every logic module tested.) Consistent with repo convention, but the convention's blind spot is now large.
- **Clean, verified live:** generated-client regen pipeline re-run at HEAD — regenerated 4 files, `git status` empty, zero drift, no hand-edits hiding in `generated/`; idempotency-key discipline held in both repos; problem-code-only branching held everywhere (zero prose matching); FE import discipline held; mobile op-dispatch converged (exhaustive `never` guard, injectable, tested).

### Diff-scope review (bmad-review lenses: adversarial, edge-case, verification-gap)

Three lens subagents ran over the staged 29.4k-line combined diff (two earlier attempts stalled and were relaunched on 2026-09-10). Findings consolidated below per lens; overlaps between lenses are noted rather than deduped.

#### Adversarial lens — 15 findings

| # | Location | Trigger | Consequence if unaddressed |
| --- | --- | --- | --- |
| A1 | `qc.command.ts` releaseHold origin re-read | Origin-bin re-read selects `{id, code, retiredAt}` without `.for('update')` (placeHold/mergeBin/retireBin all lock first) | A retireBin racing the release wins the empty gate and retires the origin bin; release then appends `qc.released` movements into a retired bin — stock stranded, no repair path |
| A2 | `receiving.command.ts` decideOverReceipt approve arm | Approve never re-validates PO/line status | An over-receipt pending at PO close can still be approved days later; `received_qty` on a closed, cancelled line grows; ledger contradicts the closed-PO record |
| A3 | `receiving.command.ts` decideOverReceipt ceiling check | Cumulative-ceiling guard reads `receivedQty` unlocked, compares absolutely before atomic += | Two concurrent approvals of separate excess rows on one line jointly push past `MAX_GRN_LINE_QTY` (int4 overflow 500 mid-tx); the new ceiling test only covers single approval |
| A4 | `po.command.ts` amend | Amend deletes/rewrites PO lines with no `receivedQty > 0` guard | A received line can be deleted or re-pointed at another SKU while open — `goods_receipt_lines`/`over_receipts` dangle, openQty flips negative unvalidated |
| A5 | `tenancy/receiving-bin.ts` ensureReceivingBinInTx | Post-onConflict re-select filters only code='RECEIVING', missing `systemOwned = true` (its QC mirror carries it) | A user bin coded 'RECEIVING' created before first GRN gets adopted: intake bin mergeable/blockable/retirable mid-flight, invisible to putaway-suggestion filters |
| A6 | `qc.command.ts` placeHold system-bin gate | Refuses only the QC-HOLD code as hold origin; system Receiving bin passes | Holding the Receiving bin quarantines receipt-side stock; task derivation and device inbox churn on stock that was never meant to be holdable |
| A7 | `qc.command.ts` placeHold scope read | `qcScopeOnHandInTx` reads (sku, bin) on-hand unlocked before arms append | Concurrent placement after the read leaves unheld residue at origin — ATP undercounts, release returns only held arms |
| A8 | `enrollment.command.ts` badgeIn PIN | 4–6 digit PIN vs scrypt with no attempt counter/lockout/rate limit | PIN space brute-forceable with no lockout to mint operator-bound sessions |
| A9 | `enrollment.command.ts` badgeIn binding | First badge-in binds any active user by email — no role gate (selfTestEcho has one) | An accountant who badges in first permanently binds as the device's single operator |
| A10 | `enrollment.command.ts` enroll auth-time replay | Replay lookup filters idempotencyKeys by key only, no tenantId predicate (every in-tx replay pairs them) | Cross-tenant ULID collision → 422 keyed off another tenant's row; unauthenticated existence oracle |
| A11 | `bin.command.ts` retireBin empty gate | Empty gate checks stock/batch projections only — no serial arm (mergeBin cross-checks serials) | A serial-projection-de-synced bin retires 'empty', stranding serials in a retired bin |
| A12 | receiving/putaway `assertUtc` + mobile `confirmPayload` | occurredAt validated as format only, no drift bound; mobile stamps from untrusted device clock | Ledger events inherit device-clock drift or forged timestamps with no detectable seam |
| A13 | `receiving.facade.ts` getCatalogSnapshot | Snapshot composes four reads across multiple transactions + `Promise.all` fan-out, no single read snapshot | Bin retired / PO closed between reads seals a self-inconsistent device snapshot; nested pool acquisitions under badge-in pressure |
| A14 | wms-fe `use-inbound.ts` usePurchaseOrders | After list resolves, one detail request per row via `Promise.all` (same in GRNs/OverReceipts) | Page of 50 → 50 unthrottled requests per render, hammering the API on every refetch |
| A15 | wms-mobile `app/revoked.tsx` reset button | `deviceStore.reset()` purges outbox + secrets directly in onPress, no confirm (web's RevokeButton gates on confirm) | One accidental tap destroys all quarantined scan data with its attribution |

#### Edge-case lens — 15 findings (path-traced; overlaps with adversarial noted in the consolidation line)

| # | Location | Unhandled path | Consequence if unaddressed |
| --- | --- | --- | --- |
| E1 | `receiving.command.ts` decideOverReceipt approve arm | Approve decided after PO/line closed or cancelled — **overlaps A2** | Excess + `received_qty` applied against a closed/cancelled PO; submit-arm gate bypassed |
| E2 | `receiving.command.ts` GRN code generation | Tenant's 999999999th GRN: next code exceeds the 9-digit regex cap | `max` stays at 999999999, every submit writes `GRN-1000000000`, unique index 409s forever |
| E3 | `tenancy/receiving-bin.ts` ensureReceivingBinInTx | User bin coded RECEIVING before first GRN — **overlaps A5** | Intake stock lands in a user bin; putaway derivation (code+systemOwned) sees no receiving bin, tasks never derive |
| E4 | `qc.command.ts` releaseHold | Concurrent retireBin/mergeBin commits between origin read and release movements — **overlaps A1** | Retire sees an empty bin; release commits stock back into a retired bin — orphaned |
| E5 | `enrollment.command.ts` badge-in PIN | Repeated wrong-PIN attempts on a known device id — **overlaps A8** | Unlimited guesses of a 4–6 digit PIN mint 30-day operator bearer tokens |
| E6 | `po.command.ts` amend | Amend omits a line whose receivedQty > 0 — **overlaps A4** | Received history orphaned; openQty loses already-received units silently |
| E7 | `receiving.facade.ts` getCatalogSnapshot | Concurrent snapshots each hold a tx plus three nested facade txs — **overlaps A13** | Pooled connections exhausted under badge-in refreshes; requests hang to timeout |
| E8 | `putaway.facade.ts` putaway-task derivation | Serial-tracked SKU's GRN line yields a derived putaway task | Unfulfillable task: `grn.received` creates no serials, putaway needs serials in Receiving bin |
| E9 | wms-mobile `src/receiving/draft.ts` canConfirm | Confirm allowed while a batch-tracked line sits at batchCode null | Whole GRN op queues then 400s at replay — quarantined after operator confirmation |
| E10 | wms-mobile `src/state/catalog-snapshot.ts` | Seal holds valid JSON of the wrong shape ('null', array, object without skus) | `snapshot.skus` undefined — resolveBarcode's `.filter` crashes receive/putaway screens |
| E11 | wms-mobile `src/scanning/adapters.tsx` | Camera re-reads an identical barcode within 1500 ms across frames | Fast deliberate repeat scans of identical labels dropped — scan-increment quantities undercount |
| E12 | wms-mobile `src/offline/sqlite.ts` listOps | One outbox row fails sealed-payload decryption | Queued GRN/placement silently vanishes — stock never recorded, operator sees a drained queue |
| E13 | wms-mobile `src/state/device-store.ts` refreshes | Badge-in's fire-and-forget refresh overlaps an explicit refreshCatalogSnapshot | Different warehouses' snapshot/lastWarehouseId pair interleaved — draft warehouse mismatches cache |
| E14 | wms-mobile `src/putaway/draft.ts` evaluateBin | Target bin retired/merged after the snapshot was cached (CatalogBin carries no retiredAt) | Placement into a retired bin queues, only 400s `bin-retired` at replay |
| E15 | `enrollment.command.ts` badge-in *(claim finding, medium)* | Claim "every mutation idempotent with same-key replay" — badge-in binds operator with no idempotency key or replay lookup | A timed-out badge-in retry re-executes the bind; no replay snapshot exists to re-serve |

#### Verification-gap lens — 5 findings (each verified against the in-repo suites: no test would fail if the behavior inverted)

| # | Gap | Evidence of absence | Consumer / consequence |
| --- | --- | --- | --- |
| V1 | Receiving-bin identity adoption: `ensureReceivingBinInTx` re-selects by code alone with no `systemOwned` filter (its 3.4 mirror has the filter); `createBin` reserves no codes; no e2e arm creates a user bin coded RECEIVING/QC-HOLD | Read both ensure helpers + `createBin`; repo-wide grep for tests creating system-named user bins: none | Impostor bin: receipts park in a non-system bin invisible to `getPutawayTasks` (systemOwned filter), blockable/retirable — **overlaps A5/E3** |
| V2 | `ensureQcHoldBinInTx` with a user bin coded QC-HOLD: filtered re-select finds nothing → bare `Error('qc-hold bin missing after ensure')` → 500 internal-error, permanently (retirement reserves the code forever, per `bin-admin.spec.ts:589-592`) | No suite creates a QC-HOLD-coded user bin | One-time bin creation bricks QC hold/release for the warehouse with unexplained 500s |
| V3 | `decideOverReceipt` approve arm never reads PO status or line status (closed-PO rule exists only on the submit path) | `receiving.spec.ts` covers approve only against an open PO; closed-PO arms only for `grn.submit` | Approval bumps a closed (possibly cancelled) PO line's `received_qty` — undefined-by-verification — **overlaps A2/E1** |
| V4 | Mobile `DeviceStore.replay()` classification: `device-revoked` → quarantine queue + revoked phase; `unauthenticated` → stay queued; else retracted. The cross-repo string contract is asserted on neither side together | device-store.test.ts tests only parseCatalogSnapshot; engine tests inject synthetic kinds, never `ApiProblem` | Code drift on either side → revoked device silently *retracts* (permanently deletes) every queued floor op instead of quarantining |
| V5 | `releaseHold` gates the origin bin on existence + `retiredAt` only — no blocked (or capacity) check; `setBlocked` has no open-hold gate | `qc-holds.spec.ts` has zero `blocked` matches; sibling commands (putaway.place, mergeBin, setBlocked) all refuse blocked bins | A hold released after the origin bin was blocked (or refilled) lands stock in a bin excluded from every operational surface — cross-story 3.4×3.6 interaction unpinned in both directions |

#### Consolidation — overlaps and unique signal

35 lens findings total (15 + 15 + 5). **Overlap clusters (independent lenses converging — strongest signal):**
- **Over-receipt approve against a closed PO/cancelled line** — A2 + E1 + V3 (three lenses, three methods).
- **Receiving-bin adoption of a user-coded RECEIVING bin** — A5 + E3 + V1.
- **releaseHold unlocked origin read racing retireBin** — A1 + E4.
- **badge-in PIN brute-force** — A8 + E5.
- **PO amend deleting/rewriting a received line** — A4 + E6.
- **Catalog-snapshot multi-tx fan-out** — A13 + E7 (connection pressure + self-inconsistent seal).

**Unique, no-lens-overlap findings worth routing separately:** E2 (GRN sequence exhaustion), E8 (serial-tracked derived task unfulfillable), E9 (mobile confirm with null batchCode), E10 (wrong-shape seal crash), E11 (identical-barcode dedup), E12 (undecryptable outbox row silently dropped), E13 (refresh interleave), E14 (placement into retired bin from stale snapshot), E15 (badge-in non-idempotency claim), A3 (over-receipt ceiling race), A6/A7 (hold on system Receiving bin / unlocked scope read), A9/A10 (badge-in role gate, enroll replay tenant predicate), A11 (retireBin serial arm), A12 (occurredAt device-clock drift), A14 (FE per-row request fan-out), A15 (mobile reset without confirm), V2 (QC-HOLD code wedge → permanent 500), V4 (replay classification cross-repo contract), V5 (release into blocked bin).

## Behavior verification

Real-HTTP smoke against the live dev server (`bun run dev` on :3000, post-merge main), one throwaway tenant per run. Script: `/tmp/epic3-smoke2.sh`. **Result: SMOKE-OK** — the full dock-to-stock Epic 3 flow completes over real HTTP:

| Step | Observed |
| --- | --- |
| Tenant register + sign-in | tenant + accessToken minted |
| Warehouse → zone → grid | grid generates 12 bins, codes `A-01-01…A-06-02` |
| Catalog CSV import | 1 row committed, 0 failed |
| Vendor → PO | PO created with 1 line (batch-tracked SKU) |
| Device enrollment-code → enroll → badge-in | deviceToken minted; badge-in issues operator token |
| GRN | `GRN-0001` applied 10 / excess 0; batch code accepted |
| Putaway task | 1 task, qty 10, carries `batchId`/`batchCode`, `suggestedBin{binId,binCode}`, `rationale` |
| Placement | placed 10 into the suggested bin |
| Bin merge | moved `{skus: 1, units: 10}`; `source.retiredAt` set (one-way) |
| Bin retire | empty bin `retiredAt` set |
| Stock read on target | 10 onHand, bin-scoped |

**Ledger/outbox cross-check (DB):** ledger received `grn.received` (+10, batch ref) → `putaway.placed` (+10, batch) → `bin.merged` (+10, `reference_doc.kind=bin-merge` with `mergeId`, batch) — the merge emits the arm movement and the `bin-merge` reference arm per the 3.6 contract. Outbox carries the full narrative spine: `tenant.registered`, `warehouse.created`, `zone.created`, `bins.generated`, `catalog.imported`, `vendor.created`, `po.created`, `device.enrollment_code_minted`, `device.enrolled`, `grn.recorded`, `putaway.recorded`, `bin.merged`, `bin.retired`.

**Fail-closed observation:** the first run's device enroll returned **503 `device-encryption-unavailable`** because the local dev `.env` lacked `DEVICE_ENCRYPTION_KEY` — the Story 3.2 guard behaves as documented; added the key and re-ran.

**Friction recorded during the smoke (script-side, not defects):** response shapes differ per family (list surfaces wrap in `{items:[...]}`; merge returns `source/target/moved`, not `sourceBin/targetBin`; retire returns the bin row flat) — a naive smoke author must consult the generated client per endpoint; problem-details bodies carry their own `code` field, which shadows naive payload-field extraction in ad-hoc probes.

## Previous-retro follow-through

Checked each Epic 1/Epic 2 retro action item for evidence it landed during Epic 3 (`d75f45f..33f57ea` + FE/mobile/meta commits):

| Item | Disposition claimed | Evidence found | Verdict |
| --- | --- | --- | --- |
| epic-1 item 4 + epic-2 a12 — FE capability-mirror drift guard | "before Epic 3/4 adds capabilities" | No drift-guard test in `wms-fe/src/lib/api/*.test.ts` | **Not landed** — Epic 3 added `bin.retire` capability (12→13) with no guard |
| epic-1 item 6 — promote shared cursor guard layer | "first refactor pass" | `decodeCursorSafe` remains near-verbatim in 7+ files; Epic 3 added 2 more copies (enrollment.command, putaway.facade) | **Not landed — widened** |
| epic-1 item 7 — mobile generated client per AD-8 | "next mobile API story" | wms-mobile still ships hand-written `api.ts` types (3-2/3-3/3-5 extended them) | **Not landed — widened** |
| epic-1 item 8 / epic-2 a9 — real-HTTP smoke as step-05 ritual | "bmad-build template" (Sasidhar) | 3-5 and 3-6 sessions did run real-HTTP smokes during verification; template/runbook edit still pending | **In practice, not in the template** |
| epic-2 a1 — keyset cursor ms-truncation (high) | "next inventory story or fix branch" | `canonicalInstant` still `toISOString()` (ms-truncated) feeding `encodeCursor` in the stock/timeline lists (`ledger.service.ts:232`); no same-timestamp boundary test | **Not landed — widened** (Epic 3's new keyset facades inherit the pattern) |
| epic-2 a2 — grant-ceiling vs concurrent adjustment | "decide in Epic 4 story-1 spec" | Not in Epic 3 scope | **Correctly deferred to 4-1** |
| epic-2 a3 — uuid path/query-param guard mechanism | "next backend story" | New endpoints use class-validator `@IsUUID` (400 `validation-failed` observed on `putaway/tasks` sans warehouseId in this retro's smoke) but no shared mechanism, no ParseUUIDPipe adoption, regex copies not consolidated | **Partially met for new endpoints, mechanism still open** |
| epic-2 a4/a5/a6/a7/a8/a10/a11/a13/a14 — BE fixes | various | No corresponding changes in the Epic 3 diff (`.env.example` still lacks `OUTBOX_RECONCILE_POLL_MS`; spec-2-1 still reads "per-tenant chain"; no blocked-draw deferred entry) | **Not landed** (none were in Epic 3's scope, but "next story" owners never claimed them) |

**Process finding:** "next story" as an owner is not claiming work. Every Epic 2 retro item assigned to "next backend story / next inventory story" went unclaimed through all six Epic 3 stories while the pattern it targets kept replicating (2 more cursor copies, 2 more `bin-merged`-adjacent scope reads). Action item routing needs an explicit claim step — see Action items.

## Action items

Compiled from the consolidated findings. Fixes are **proposed, not auto-applied** — the human decides what to execute. Grouped so related fixes land as one branch each.

| ID | Action | Owner | Disposition |
| --- | --- | --- | --- |
| epic-3-retro-a1 | **Over-receipt approve: re-validate PO + line status in `decideOverReceipt`'s approve arm and lock the PO line before the ceiling bump** (A2/E1/V3 triple-confirmed + A3 race). One branch: `po-not-open` gate on approve, `.for('update')` on the line, e2e arm for close-then-approve | Claude (fix branch, before Epic 4 consumes over-receipts) | fix now |
| epic-3-retro-a2 | **System-bin identity: add `systemOwned` filter to `ensureReceivingBinInTx`, reserve `RECEIVING`/`QC-HOLD` codes in `createBin`/`generateGrid`, pin both with e2e arms** (A5/E3/V1/V2 — including the QC-HOLD wedge that 500s forever) | Claude (next bin/inbound story) | fix now |
| epic-3-retro-a3 | **releaseHold hardening: `.for('update')` on the origin-bin read, and pin the release-into-blocked-bin behavior** (decide: refuse like the retired sibling, or explicit 200 exemption) (A1/E4/V5) | Claude (next inbound story) | fix now |
| epic-3-retro-a4 | **retireBin serial-arm empty gate** (A11; the 3-6 review deferred rows #2/#31 already record the projection-vs-serial asymmetry) | Claude (next bin story, with a2) | fix now |
| epic-3-retro-a5 | **PO amend received-line guard** (A4/E6): refuse deletion/skuId rewrite of lines with `receivedQty > 0`; clamp `orderedQty >= receivedQty` | Claude (next inbound story) | fix now |
| epic-3-retro-a6 | **badge-in hardening batch** (A8/E5 failed-attempt lockout, A9 role gate on binding, A10 tenantId predicate on the enroll replay lookup, E15 idempotency-or-explicit-non-replay doc) | Claude (next tenancy story) | fix now |
| epic-3-retro-a7 | **Architecture guard decision: the facade-guard test is vacuous (F-4).** Either fix the regex to catch real import forms and extend to read-guards (F-5/F-6), or delete the test and relax the AD-6 docblock claim — decide once | Claude (Epic 4 story-1 spec or refactor pass) | decide + fix |
| epic-3-retro-a8 | **Scaffolding extraction pass** (F-7 `writeIdempotencyKey`×5 + cursor guard copies F-12; supersedes epic-1 item 6) and **FE primitives** (`useCursorPage` F-14, shared `rejectionReason` F-15) | Claude (first refactor pass) | defer (batched) |
| epic-3-retro-a9 | **Mobile hardening batch** (E9 batchCode-null confirm, E10 wrong-shape seal, E12 undecryptable row surfaced, E13 refresh single-flight, E14 retired-bin staleness, A15 reset confirm, F-17 duplicate reason-code consts) | Claude (next mobile story) | fix now |
| epic-3-retro-a10 | **Record the two silently-diverged 3.2 items** (F-1 claim UX, F-2 nearest-correct-bin hint + "Queued" microcopy) as deferred-work entries so Epic 4/5's picking spec inherits an explicit record | Claude (one meta-repo edit) | fix now (docs) |
| epic-3-retro-a11 | **Claim routing: every open retro item must be either claimed into a story spec or explicitly closed at the next epic boundary** — "next story" as owner let all Epic 2 fix-now items go unclaimed while their patterns replicated | Sasidhar (sprint ritual) | process |
| epic-3-retro-a12 | **Measure the 1.5 s/500 ms scan budgets on a real mid-range Android** (F-3) and record the numbers in spec-3-2 or deferred-work | Sasidhar (device in hand) | defer to when a device is available |
| epic-3-retro-a13 | **Catalog-snapshot single-tx composition** (A13/E7): one read transaction + bounded fan-out per badge-in | Claude (next receiving story) | fix now |
| epic-3-retro-a14 | **Small guards**: GRN sequence-exhaustion 409 (E2); occurredAt drift bound decision (A12); `use-inbound.ts` tests (F-18) | Claude (respective next stories) | defer (batched) |

## Acceptance verdict

**accepted-with-open-items** (declared criteria — epics.md per-story AC blocks).

Basis: `pending_stories` empty (all six stories done); reconciliation 20 met / 4 met-with-recorded-deferral / 1 reinterpreted-with-record against 27 audited AC items; behavior check passed end-to-end over real HTTP with ledger/outbox cross-check; 305 BE / 77 FE / 69 mobile tests green post-merge. The open items — 2 silently-diverged Story 3.2 AC halves (both currently vacuous pending Epic 4/5 picking), the unmeasured scan budgets, and the lens findings (correctness gaps concentrated in over-receipt approve, bin identity, and release/retire races) — do not undermine the epic's declared criteria, but several are fix-before-Epic-4: Epic 4's reservation consumers sit directly downstream of the over-receipt, ATP, and putaway-suggestion surfaces where the gaps live.

## Open questions

1. **AD-6 read policy** (F-5/F-6): do cross-module *table reads* get routed through facades, or is the docblock claim relaxed to "writes only"? Affects every future command family; decision belongs in the Epic 4 story-1 spec.
2. **badge-in binding policy** (A9): which roles may bind a device as its first operator — floor-permitted set, or any active user?
3. **occurredAt policy** (A12): accept device-clock drift as recorded truth, bound it (±24h), or stamp server-side at ingest with device time in a side field?
4. **Reserved bin codes** (a2): hard-reject at `createBin` (409) vs adopt-with-`systemOwned`-filter only?

## Acceptance verdict

_(pending)_

## Open questions

_(pending)_