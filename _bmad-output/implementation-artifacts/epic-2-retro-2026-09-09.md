---
epic: 2
date: 09-09-2026
verdict: accepted-with-open-items
criteria: profiled
headless: false
---

# Epic 2 Retrospective — Inventory Backbone: One Trusted Number

## Epic summary

- **Epic:** 2 — Inventory Backbone ("the system Priya can trust at 11:59 PM", AD-1/2/11/12/16). FRs covered: FR-4 (ledger core), FR-5 (ATP + reservations), FR-6 (batch/serial traceability).
- **Diff range:** `0a5b519..d75f45f` in wms-be (parent of the first story commit through the last) — 7 non-merge commits, no merges:
  - `c9e0c93` (#8) — 2-1 ledger core; `ee543d9` (#9) — outbox substrate + relay; `b0b14ad` (#10) — 2-2 reconciliation; `5081f75` (#11) + `a948567` (#12) — 2-3 ATP/reservations incl. review-loop fixes; `2dbbce7` (#13) — 2-4 traceability; `d75f45f` (#14) — 2-5 read surfaces.
- **Stories completed:** all 5 sprint-mode stories `done` (`pending_stories` empty per `detect-epic --epic 2`), plus the untracked-in-sprint `outbox-relay` spec (`spec-outbox-relay.md`, PR #9) that landed between 2-1 and 2-2 and is included in the range.
- **Evidence inventory:**
  - Epic spec: `planning-artifacts/epics.md` epic list (epic goal + FR coverage only — **no per-epic acceptance criteria declared** → verdict is **profiled** from the stories and diff).
  - Story specs: `spec-2-1…2-5` (all status `done`, each with a frozen intent/boundaries block, I/O matrix, and a filled Review Triage Log).
  - Compiled context: `epic-2-context.md`, `epic-1-context.md`.
  - git_evidence pre-pass: 66 files changed, ~13k net lines; largest churn: `ledger.service.ts` +1667 net, `openapi.json` +1315 net, `inventory.controller.ts` +715, `inventory.facade.ts` +629, `inventory.dto.ts` +571, `inventory.command.ts` +485; test suites: `reconciliation.spec.ts` +1111, `batch-serial.spec.ts` +1109, `reservations.spec.ts` +1153, `ledger.spec.ts` +853, `inventory-surfaces.spec.ts` +593, `outbox.spec.ts` +680.
  - FE side: 2 client-regen commits only (`d9d3d02` #7, `2c1f824` #8) — no epic-2 FE feature story.
  - Meta: contract docs updated per story (merged PRs #4–#9).
  - **Missing evidence — session logs:** no persisted session records for the story sessions (conversation-only); the process-lesson analysis is narrowed accordingly.
  - **Missing evidence — declared acceptance criteria:** the epic spec declares no per-story acceptance gate; the verdict is profiled.

## Findings

Method: five independent derivation/review passes over the full epic diff (`/tmp/epic-2-diff.patch`, 0a5b519..d75f45f) — aggregate views (architecture delta, duplication map, god-class growth, pattern divergence), spec-to-implementation reconciliation across the six specs, and three review lenses (blind / edge-case / verification-gap), each anchored to file:line — then a live behavior check over real HTTP (see next section). Findings are deduped and grouped; every one carries its source.

### Correctness / contract findings (backend)

| # | Finding | Severity | Source | Live-confirmed? |
|---|---------|----------|--------|-----------------|
| F1 | **Keyset pagination silently skips rows at page boundaries.** The cursor encodes `created_at` truncated to milliseconds (`canonicalInstant` → `new Date(v).toISOString()`, `ledger.service.ts:232-234`) while SQL orders/compares on full-microsecond `timestamptz` (`inventory.facade.ts:282-284, 342-353`). All N per-unit events of one serial adjustment share one `now()` timestamp; a page ending mid-timestamp encodes the truncated instant, and rows with `.123456 > .123` fail the `<` boundary — the id tiebreaker is bypassed and events are never returned by any page. Affects both the timeline and the stock list. | high | edge lens | not reproduced live (needs same-ms page boundary) |
| F2 | **Grant ceiling race vs concurrent adjustment.** `grant` reads `committedCeiling` in a probe tx that closes before the Valkey Lua script runs (`reservation.service.ts:254-295`); an adjustment committing in that window is invisible, so `reserved + qty ≤ ceiling` passes against stock that no longer exists. The counter arbitrates grant-vs-grant only — nothing arbitrates grant-vs-stock; the module header's "never oversells" claim (`reservation.service.ts` header comment) is stronger than the mechanism. ATP clamps at 0 so the counter doesn't compound, but a successful hold can name units that never exist. | high | blind lens + edge lens | not reproduced live (facade-only; needs concurrent harness) |
| F3 | **Malformed `warehouseId` on the three warehouse-scoped read routes → 500 `internal-error`** while the routes document 404 for unknown warehouse (`inventory.controller.ts:385-517` has no uuid guard; contrast the `UUID_RE` guards the 2-5 patch added on batchId/serialId at :545/:596). The repo-wide `ParseUUIDPipe` deferral (from 2-1) is the root. | medium | edge lens | **YES** — live: `GET …/warehouses/not-a-uuid/inventory/stock` → 500 |
| F4 | **The multi-serial adjustment response misrepresents the ledger row it names.** Response returns the last per-unit event's id/seq with the aggregate `quantityDelta` (`inventory.command.ts:433-477`): live probe of a 3-serial intake returned `{seq: 6, quantityDelta: 3}` while the timeline shows seq 6 with `quantityDelta: 1` — the event object described in the response's OpenAPI contract does not exist in the ledger with that delta. Outbox payload repeats the same pairing. | medium | blind lens + edge lens | **YES** — live: response says 3 at seq 6, timeline says 1 |
| F5 | **`.env.example` omits `OUTBOX_RECONCILE_POLL_MS`** while carrying the other two workers' variables with "must set" comments (`.env.example:18,24,30` vs `jobs.module.ts` parse). A deployment following the example boots relay + reaper but never the reconciliation worker — the ledger-vs-projection drift detector is silently off. | medium | blind lens | **YES** — live: `.env.example` grep + no reconcile worker log |
| F6 | **A poisoned never-checkpointed partition starves reconciliation for all other partitions.** The failure-requeue stamp (`reconcile.ts:316-339`) only helps partitions that have a checkpoint row; `pickPartition` orders `updated_at asc nulls first` (`:354-371`) and a partition with no row is re-picked first every tick with nothing to stamp — the code's own comment contradicts its anti-wedging goal. | medium | edge lens | no |
| F7 | **One error code, two HTTP statuses:** `unavailable` serves both the 409 deterministic-loser and the 503 fail-closed ATP path (`reservation.service.ts` grant path vs `:446-448`) — no other Epic 2 code splits this way; Epic 4 consumers will branch ambiguously. | medium | spec reconciliation | no |
| F8 | **UUID validation is three different mechanisms across the epic:** 2-3's `requireUuid`→400, 2-5's ad-hoc `UUID_RE`→404, and the unguarded path/query params still 500-ing (F3). The repo-wide `ParseUUIDPipe` deferral stays open with drift accreting around it. | medium | spec reconciliation + aggregate views | no |
| F9 | **Verified verification gaps** (each: the promise, where it lives, and the mutation that goes undetected): (a) `exportDigest`'s three rejection arms (`ledger.service.ts:975-978, 997-999, 1006-1009`) — deleting the contiguity check leaves the ledger.spec tamper-probe gap silently digestable, all tests green (high); (b) `anchorChain`'s empty-ledger refusal (`:881-888`) and its concurrent-overlap advisory lock (`:867-868`) — the seq-allocation lock's twin got a concurrency test, the anchor lock got none (medium); (c) outbox ack-delete / failure-bookkeeping resilience catches (`outbox.ts:202-243`) — no test injects a failing delete or bookkeeping write (medium); (d) reconciliation failure-requeue stamp (`reconcile.ts:322-335`) — the worker tests stub the facade so the catch/stamp block never runs (medium); (e) `parseLastDivergences` malformed-entry guard (`reconcile.ts:71-100`) — a `null` entry would TypeError-wedge every future cycle (medium); (f) `lastError` 2000-char truncation (`outbox.ts:283-284`) (low); (g) quarantine budget of 5 is self-referentially asserted — tests re-derive `OUTBOX_MAX_ATTEMPTS` instead of pinning it (low). | medium (cluster) | verification-gap lens | no |
| F10 | **`blocked` batch/serial status is paper-only and unenforced:** declared in DDL CHECK + OpenAPI enum (migration 0010), no code writes it, and the FEFO default draw never consults it — blocked stock would be default-drawn with no `overrideReason`, defeating the override audit trail; today additionally unreachable. Recorded in 2-4 as Epic 3/4 scope, but the draw-side unenforcement was never recorded. | low | blind lens + spec reconciliation | no |
| F11 | **Grant accepts `ttlSeconds: 0`** → `expires_at = now()`, a hold immediately reapable; caller's commit then 409s (low). | low | blind lens | no |
| F12 | **Unbounded detail histories over HTTP** (`serialHistory`/`batchHistory`, no LIMIT, full `eventHash` per row) — long-tail growth concern, triage-rejected in 2-5 as frozen-matrix behavior ("full history, one query"). Carried here so Epic 4's UI story inherits it consciously. | low | blind lens | no |
| F13 | **`verifyChain` with a degenerate range (fromSeq > toSeq, or 0) emits a false SEVERITY-1 `ledger.chain_broken` alert** instead of rejecting input — contrast `exportDigest`'s range validation (`ledger.service.ts:1562-1599` vs `:974-981`). No HTTP surface today; false alarms erode the severity-1 signal. | low | edge lens | no |
| F14 | **A same-key/different-payload concurrent request creates catalog identity rows before its 422** — the "replay pre-check … BEFORE any identity creation" claim holds only for committed originals; an in-flight original lets the composition's `ensureBatches`/`ensureSerials` commit rows that the rejected request leaves behind (`inventory.controller.ts:140-149`, `catalog.facade.ts:205-294`). Harmless today (idempotent identity), but the claim is wrong for the interleaving. | low | edge lens | no |
| F15 | **Upper int4 boundary on a repeated max-magnitude intake 500s** — the projection fold has no ceiling guard (`ledger.service.ts:626-654`; `stock_on_hand.quantity` has only a non-negative CHECK), so a second `quantityDelta: 2147483647` on one bin raises `22003 integer out of range` as an untyped 500. | low | edge lens | no |
| F16 | **A pre-checkpoint divergence on an untouched scope is never re-examined** — the bounded-scan comment's escape hatch ("caught by a full pass") is unreachable in normal operation: full passes happen only on the first cycle and a corrupt-checkpoint discard (`reconcile.ts:505-512`, `ledger.service.ts:1259-1308`). The divergence persists until an operator runs a rebuild. | low | edge lens | no |

### Structural / process findings (aggregate views)

| # | Finding | Source |
|---|---------|--------|
| S1 | **Duplication accreted along commit seams, not within them:** 6 file-local copies of the identical UUID regex (`inventory.facade.ts:147`, `inventory.controller.ts:621`, `reservation.service.ts:31`, `tenancy.service.ts:403`, `sku.command.ts:59`, `users.command.ts:618`) while the shared `isUuid()` primitive (`shared/primitives/ids.ts`) goes unused; 4 near-identical `decodeCursorSafe` copies with a strictness divergence (inventory.facade's strict regex vs epic-1's `Date.parse` looseness) and three construction styles for the same 400; 3 ISO-instant normalizers (`canonicalInstant`, `canonicalDate`, `assertUtcIso`); the ~28-line deployment-parity test bootstrap block copy-pasted into 9 suites in 3 divergent variants (advisory-lock key 742105 vs 742106 ad hoc, no registry); three byte-similar worker skeletons + poll parsers inlined in `jobs.module.ts`. No shared test helper module exists. | aggregate views |
| S2 | **God-class growth is deliberate but at its ceiling:** `ledger.service.ts` 852→1667 lines across five concerns (append+fold, serial locking, replay/repair, chain verify/anchor, digest) — pinned to single-owner status by `architecture.spec.ts:110-136`, which is what keeps it cohesive and also what keeps it accreting; `inventory.facade.ts` 629 lines / 24 methods across four concern areas; `inventory.controller.ts` 714 lines with ~290-line `adjustStock` holding business policy (FEFO resolution, batch-coverage assertion) in the api shell — epic-1 controllers passed through to command services. `reservation.service.ts` (948) is cohesive but at the single-responsibility ceiling. Test mass: ~6200 epic-2 test lines vs ~2950 for all of epic 1. | aggregate views |
| S3 | **Two coexisting cross-module consumption idioms:** catalog declares its tenancy dependency in the Nest module graph (`forwardRef`, `catalog.module.ts:29`) while inventory consumes tenancy file-level functions with no module-graph edge (`inventory.module.ts:21-24` documents this as deliberate) — the conventions diverged and neither is pinned by the architecture test. Also: `shared/valkey/` holds reservation-specific key namespaces and Lua scripts whose only consumers are inventory-module files (single-consumer shared layer); `reconcile.ts` breaks the `*.service.ts` naming convention (class is `ReconciliationService`); reservation error construction uses a private class method (`terminalNotFound`) where every other module uses module-level helper functions. | aggregate views |
| S4 | **Spec drift never reconciled into the frozen text:** 2-1's frozen "hash chain per-tenant" (`spec-2-1` L27) vs the as-built per-(tenant, warehouse) chain — every later story assumes warehouse granularity, the frozen intent was never amended. Also the 2-2 checkpoint's `last_divergences` column and four 2-2 behaviors (failure re-stamp, partition discovery clause, discard-clean re-earn, parse hardening) and several 2-3 bounds (10-year TTL ceiling, 7-day counter backstop) are triage-provenanced but absent from the frozen matrices — traceable, but only through triage logs, not through the specs a future reader would read first. | spec reconciliation |
| S5 | **Deferral closure health is good where it was owned, weak where it wasn't:** the two epic-2-resolved deferrals landed exactly as promised (verify-before-anchor in 2-2; the 2-4 OpenAPI failure surface in 2-5). But of the deferred-work ledger, 9 epic-2-originated entries remain open at HEAD (`idempotencyKeyReuse` misplacement with its cross-module import, `ParseUUIDPipe` absence, RLS `::uuid` cast hardening in 0006–0010, `db:verify` app_metadata-only round-trip, 0007 snapshot `isRLSEnabled:false`, DLQ runbook doc, hung-`drain` pool timeouts, the `verifyChain` fail-loud test) — several accreted new drift during the epic (F3/F8 grew out of the `ParseUUIDPipe` deferral). Pre-epic-1 entries (mobile client, `skus` keyset index, ROLE_CAPABILITIES drift guard, 0005 backfill verification) are untouched by range, as expected. | spec reconciliation |

## Behavior verification

Live HTTP run against `bun run dev` on :3000 (dev Postgres `wms-be-db` on :55432, valkey on :56379), 2026-09-09 ~12:38 UTC. Full fresh-tenant journey exercised, not synthesized fixtures:

- **Boot:** with `.env` as committed, the reservation subsystem fails closed loudly (`VALKEY_URL is required for the reservation paths`) while the app itself boots — correct fail-closed, but the local `.env` was missing a variable `.env.example` declares (fixed for this run by appending `VALKEY_URL=redis://localhost:56379/0`). After the fix: clean boot, `Reservation counters rebuilt from journal … (39 tenant(s))` — the 2-3 boot-rebuild ran against the real journal.
- **Workers:** relay, reconciliation, and reaper are env-gated off in this dev run (no `OUTBOX_*`/`RESERVATION_*` in `.env`) — no outbox drain and no reconciliation occurred while the epic's write flows were exercised; consistent with the gate design, and F5 means a deployment that sets the example's variables still misses reconciliation.
- **Write flows (POST adjustments, real HTTP, fresh tenant):** untracked intake +10 → 201 with correct onHand snapshot; over-draw −99 → 422 `insufficient-on-hand` naming the bin; batch intake of two batches (one with a past expiry) across two bins → per-batch onHand 5/5; combined batch+serial intake of 3 serials → exactly 3 per-unit ledger events (seqs 4-6, each ±1); untracked draw −3, explicit-batch draw −2 with `overrideReason`, FEFO default draw −1 → all 201 with correct projections.
- **Policy arms observed live:** FEFO default draw on a bin whose only batch is expired refuses with 422 `insufficient-on-hand` ("holds no non-expired batch stock … name a batch explicitly") — expired stock is never silently drawn (2-4 draw policy holds); duplicate serial re-intake → 409 `duplicate-serial` naming the serial and its bin; unknown-serial draw → 404 `serial-unknown`; explicit-batch draw without `overrideReason` → 400 `validation-failed`; idempotent replay (same key, same body) → byte-identical seq/onHand; same key, different body → 422 `idempotency-key-reuse`.
- **Read surfaces (all four new routes):** timeline returns 10 items with `batchRef`/`serialRef`/`referenceDoc` enriched exactly where the arms were (per-unit serial events carry both refs; plain/untracked carries none); stock list returns the four settled scopes (9/5/2/3) with `nextCursor` exhausted correctly; batch list orders FEFO with the expired batch **listed first** (B-OLD, 5) ahead of B-NEW (2) — order is data, draw policy is separate (Design Note holds); batch detail returns identity + per-bin rows + history; serial detail returns derived location (the intake bin) + history.
- **Error arms on reads:** malformed batch id → 404 (the 2-5 patch works); malformed serial id → 404; invalid cursor → 400 `invalid-cursor`; unknown well-formed warehouse → 404; unknown skuId filter → 200 `{items: []}`; missing skuId → 400. **Exception: malformed `warehouseId` → 500** (F3, live-confirmed).
- **Snapshot mismatch live-confirmed:** the 3-serial intake's response said `{seq: 6, quantityDelta: 3}`; the timeline shows seq 6 as `quantityDelta: 1` with `serialRef` set (F4).

Passing tests do not substitute for the above: two of the live confirmations (F3, F4) correspond to behaviors the 212-test suite never observed, and F5's missing env var is invisible to any test.

## Previous-retro follow-through

Epic 1 retro (`epic-1-retro-2026-09-08.md`) carried 9 action items into Epic 2. Per-item evidence:

| Epic-1 item | Disposition | Evidence |
|---|---|---|
| 1 (BE docs: `role-denied` on 403s) | landed (its own owner) | meta PR — tracked done in sprint status during epic 2 |
| 2 (meta contract docs: accept-invite + replay carve-out) | landed | same |
| 3 (Epic 2 outbox: replay-suppression parity for users publishes) | **landed in Epic 2** | `ee543d9` (outbox substrate PR #9) carries the suppression-parity comments in `users.command.ts` ("a replayed invite writes NO second outbox row"); sprint-status item should move to `done` |
| 4 (FE capability-mirror drift guard before Epic 2 adds capabilities) | **not landed; drift already accreted** | Epic 2 added the `stock.adjust` capability on the backend; `wms-fe/src/lib/users.ts:28` `ROLE_CAPABILITIES` has no `stock.adjust` — the guard-less mirror has drifted exactly as the item predicted |
| 5 (migration 0005 owner-backfill verification) | not landed (out of epic range, as expected) | open in sprint status |
| 6 (promote `decodeCursorSafe` into shared pagination) | **not landed; new copy added** | 2-1 added a 4th copy (`inventory.facade.ts:163`, strictest of the four) instead of promoting; item 6's "first refactor pass" window has now accreted more divergence (S1) |
| 7 (mobile generated client) | not landed (no mobile story in Epic 2) | open |
| 8 (real-HTTP smoke ritual in step-05) | **not landed as process, but practiced here** | this retro's behavior check is the first live-HTTP run at an epic boundary; two of its confirmations (F3, F4) were invisible to the 212-test suite — evidence for the ritual, not execution of it |
| 9 (retro at every epic boundary) | **landed** | this document; item can move to `done` |

## Action items

Proposed for the sprint's `action_items` ledger (proposed, not auto-applied — the human decides what to execute):

| # | Action | Owner (proposed) |
|---|--------|------------------|
| A1 | **Fix the keyset cursor to full timestamp precision** (F1, high): encode the raw `timestamptz` (microsecond string) in the cursor instead of the ms-truncated ISO instant, or compare with `>= trunc` + id tiebreak in the right direction; add a same-timestamp page-boundary walk test to `ledger.spec.ts`/`inventory-surfaces.spec.ts`. Backend-first; the cursor is opaque to the FE. | Claude (next inventory story or a fix branch) |
| A2 | **Close the grant-ceiling gap** (F2, high): either re-validate the ceiling after the script win before the journal insert (read committed on-hand again inside the grant tx), or weaken the module header's "never oversells" claim to match the actual guarantee (counter arbitrates grant-vs-grant; stock arbitration lands with Epic 4's consuming commit). Decide which in the Epic 4 spec, not now. | Claude (Epic 4 story-1 spec) |
| A3 | **Guard or document every uuid path/query param** (F3+F8, medium): pick one mechanism (the repo-wide `ParseUUIDPipe` deferral from 2-1) — either adopt `ParseUUIDPipe` globally (400) or extend the 2-5 `UUID_RE` guard pattern to warehouseId on the three warehouse-scoped routes at minimum; consolidate the 6 regex copies onto one shared constant (S1). | Claude (next backend story) |
| A4 | **Fix the multi-serial response snapshot** (F4, medium): return the aggregate snapshot WITHOUT naming a ledger row id/seq (or return the first per-unit event and let the timeline carry the rest); fix the outbox payload pairing; add the timeline-vs-response consistency assertion to `ledger.spec.ts`. OpenAPI + FE client regen ride along. | Claude (fix branch) |
| A5 | **Add `OUTBOX_RECONCILE_POLL_MS` to `.env.example`** (F5, medium) and reconcile the local `.env` (VALKEY_URL was missing in dev too — append the example's full worker section). One-line fix, deploy-affecting. | Claude (fix branch, with A4) |
| A6 | **Reconciliation starvation guards** (F6+F16, medium): stamp/re-queue never-checkpointed partitions on failure (synthetic updated_at or a separate queue arm), and schedule a periodic full pass (e.g. a max-age on checkpoints) so the bounded scan's "full pass" escape hatch is reachable. Pair with the G7 `parseLastDivergences` guard test and the G6 stamp test. | Claude (next reconciliation-touching story) |
| A7 | **Close the verification gaps** (F9): the six verified mutations each deserve one test — exportDigest's three refusal arms, anchorChain's empty-ledger arm + a concurrent-anchor test, outbox ack-delete/bookkeeping failure injection, the reconcile failure-stamp, `parseLastDivergences` malformed entry, and pinning `OUTBOX_MAX_ATTEMPTS = 5` as a literal in the quarantine test. | Claude (verification pass, can be batched into one PR) |
| A8 | **Settle the `unavailable` double-meaning** (F7, medium): give the 503 fail-closed path its own code (e.g. `reservation-store-unavailable`) before Epic 4 consumers branch on it. | Claude (Epic 4 story-1 spec) |
| A9 | **Process: make the real-HTTP behavior check a step-05 ritual** (Epic-1 item 8, now with evidence): the live run cost one env-var fix + ~15 curl calls and caught two behaviors 212 tests did not (F3, F4). Fold a minimal smoke script into the bmad-build verification runbook. | Sasidhar (bmad-build template) |
| A10 | **Promote shared test bootstrap + lock-key registry** (S1): extract the 9-suite deployment-parity block into `test/helpers/` and give advisory keys (742105/742106, production string keys) a single registry file. Closes Epic-1 item 6's spirit for the test layer too. | Claude (first refactor pass) |
| A11 | **Reconcile the 2-1 frozen text** (S4): amend `spec-2-1`'s "per-tenant chain" to the as-built per-(tenant, warehouse) chain (or note the amendment in the spec's change log), so the frozen text matches what 2-2/2-4 and the anchors table assume. | Claude (one meta-repo edit) |
| A12 | **FE capability-mirror drift guard** (Epic-1 item 4, drift now evidenced): with `stock.adjust` drifted, land the guard (generated client ↔ `ROLE_CAPABILITIES` cross-check test in wms-fe) before Epic 3/4 stories consume capabilities. | Claude (next wms-fe story) |
| A13 | **Record the blocked-status draw-side unenforcement** (F10, low): add it to the 2-4 spec's design notes / deferred-work so the Epic 3/4 blocked-batch story knows the FEFO draw must consult status. | Claude (deferred-work entry) |
| A14 | **Small typed-error hardenings** (F11 ttlSeconds:0 → 400; F13 verifyChain range validation; F15 int4 ceiling → 422): batch into the next adjustment-touching PR. | Claude (next adjustment story) |

## Acceptance verdict

- **Criteria: profiled** — the epic spec declares no per-story acceptance gate; the profile comes from the five story specs' frozen intents + I/O matrices (all implemented; spec reconciliation found no silently dropped requirement in any of the six specs' frozen text beyond the recorded triage decisions), the diff, and the live behavior check.
- All 5 sprint stories `done` (`pending_stories` empty); the outbox-relay substrate spec landed inside the range as promised by the 2-1 deferral.
- The epic's core promises held under live exercise: append-only ledger with gap-free per-warehouse seqs, one trusted number (projections correct after 10 real movements including multi-arm composites), atomic reservation substrate boot-rebuilt from journal, batch/serial traceability with FEFO-as-data, and the 2-5 read surfaces returning exactly the facade truth with additive timeline fields.
- The two high findings (F1 cursor data-skip, F2 grant-vs-adjustment race) are real but bounded: F1 corrupts read pagination (the ledger itself is intact), F2 requires a concurrent harness the product cannot trigger yet (reservations are facade-only until Epic 4) — both land as open items with owners, neither invalidates the shipped behavior for the stories' declared scope.
- Open items carried forward: A1–A14 above (2 high, 6 medium-cluster, the rest low/process), plus the 9 epic-2-originated deferrals still open in `deferred-work.md` (S5).

**Verdict: accepted-with-open-items.**

## Open questions

- F2's fix shape (re-validate at journal insert vs defer stock-arbitration to Epic 4's commit path) is an Epic 4 spec decision — flagged, not decided here.
- Whether F1's cursor fix changes the FE's stored cursors (it must not — cursors are opaque and single-flight) — confirm when A1 lands.