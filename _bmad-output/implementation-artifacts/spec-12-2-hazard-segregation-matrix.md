---
story: 12-2-hazard-segregation-matrix
title: "12-2 hazard segregation matrix — FR-41 on the backend"
type: 'feature'
created: '2026-09-23'
status: 'done'
baseline_commit: '4bd00f5'
route: 'dispatch'
review_loop_iteration: 1
epic: 12
context:
  - '_bmad-output/implementation-artifacts/epic-12-context.md'
  - 'docs/design/SYSTEM-DESIGN.md'
  - 'docs/design/IMPLEMENTATION-GUIDE.md'
  - 'docs/design/modules/putaway.md'
  - 'docs/design/modules/catalog.md'
  - 'docs/design/modules/tenancy.md'
  - 'docs/design/API-SURFACE.md'
---

<frozen-after-approval reason="human-owned intent — do not modify unless human renegotiates">

## Intent

**Problem:** FR-41 — incompatible goods cannot be co-located: a segregation matrix over hazard classes refuses the placement naming both parties. 12-1's storage class is a (SKU, bin) rule; it says nothing about two hazardous SKUs sharing one bin (an oxidiser beside a fuel). Today nothing stops it.

**Approach:** SKUs gain a nullable `hazard_class` from a controlled vocabulary (CHECK-backed, the 12-1 three-layer pattern), and one symmetric pairwise predicate in a shared primitive — `hazardClassesCompatible(a, b)` — is imported by the same gated-writer set 12-1 established: the putaway suggestion walk, the placement guard, and `mergeBin`, plus a SKU hazard-edit guard mirroring 12-1's `storage-class-conflict`. The matrix is encoded ONCE in TS; the co-location rule reads the target bin's current occupants.

## Boundaries & Constraints

**Always:**
- The matching rule (the default matrix) lives once in `src/shared/primitives/hazard.ts` — no SQL-side copy, exactly the 12-1 hierarchy precedent. The candidate list stays SKU-agnostic.
- Segregation is a **co-location** rule (SKU × SKU-in-bin), unlike storage class's (SKU × bin) rule: the gate reads the *occupants'* hazard classes, so a hazard-capable SKU may still enter an empty bin of the right storage class.
- Conformance stays a command-layer rule (AD-18); every refusal names both parties and both classes.
- The SYNC HAZARD rule extends: the hazard arm must land in `candidateFitsSku` + the placement guard + `mergeBin` in one commit.

**Never:**
- No new ledger event type (a segregation refusal is a guard, not a movement).
- Bins do NOT gain a hazard column in this story — the co-location rule is occupant-based; bin-side hazard designation is 12-3/12-4 material (recorded narrowing).
- No pick-draw or wave/pool-filter change (drawing FROM a bin co-locates nothing).
- `stock.adjust` stays a named bypass (the 12-1 precedent — extend its PENDING entry).
- No mobile snapshot, no FE file, no `bins.type` writer, no QC-release logic change.
- **The decided vocabulary and default matrix** (human decision 2026-09-23): `explosive, oxidizer, flammable, corrosive-acid, corrosive-base, toxic, gas` — 7 classes. Matrix: `explosive` segregates from ALL; `oxidizer`↔`flammable` (FR-41's oxidiser/fuel example), `oxidizer`↔`gas`, `corrosive-acid`↔`corrosive-base`, `corrosive-acid`↔`toxic`. Covers the stated industries (chemicals → acids/bases/toxic, LPG → gas, ammonium nitrate → oxidizer, ammunition → explosive); 12-7's admin surface can widen it. The full spec is kept despite ~3,500 tokens (single goal; the size is design detail) — human decision 2026-09-23.
</frozen-after-approval>

## Code Map

- `src/shared/db/schema.ts` — add `skus.hazardClass` (nullable text, no default) beside `skus.storageClass` (:413); doc comment naming the shared primitive and the CHECK's migration-only home. **Bins gain nothing.**
- `drizzle/0036_*.sql` — **run `bun run db:generate` first** (ADD COLUMN + snapshot + journal row), **then hand-append `skus_hazard_class_check`** (`col IN (...) OR col IS NULL` — nullable, so the null arm is required; the 0035 convention). Forward-only.
- `src/shared/primitives/hazard.ts` (new) — `HAZARD_CLASSES` (the decided 7-class vocabulary), `hazardClassesCompatible(a, b)` (symmetric pair set + the explosive universal rule), `assertHazardClass(fields)` (the `assertStorageClass` shape — skips undefined/null so PATCH's null-clears verb survives), refusal factories: `segregationConflict(detail)` 400 `bin-segregation-conflict` (placement/merge) and `segregationStateConflict(detail)` 409 `hazard-segregation-conflict` (SKU edit guard) — the 12-1 split (device-facing refusal vs state conflict), deliberate.
- `src/modules/putaway/putaway.command.ts` — `SkuPhysicalAttributes` (:853-863) gains `hazardClass: string | null`; **`PutawayBinCandidate` (:834) gains `occupants: readonly { skuId: string; hazardClass: string }[]`** — the occupants as PAIRS, not a class list (the amended shape, review loopback 1: a class-only list cannot skip the moving SKU's own pairs, and a bare class aggregate would also carry drained residue). `binCandidatesInTx` (:924, the grouped query) aggregates the occupants' pairs through the existing stock_on_hand→skus join — **`gt(stock_on_hand.quantity, 0)` and a non-null class in the aggregate filter** (drained rows persist — `addToOnHand` upserts, never deletes — and a stale class would refuse placements the gate admits); the list stays SKU-agnostic (no WHERE arm; the projection is richer, the rule still runs per-candidate on the SKU side). `candidateFitsSku` (:929-960) gains the hazard arm AFTER the class gate: for each occupant PAIR, **skip `skuId === movingSkuId`** (the same own-SKU arm the placement and merge gates carry — the decided same-SKU-consolidation rule), else incompatible → no fit; it therefore takes a `movingSkuId` argument that BOTH call sites thread — the placement's `suggestBinInTx` re-derivation (:646, `command.skuId` is already in hand) and the facade's task derivation (`putaway.facade.ts:349`, the line's SKU). Placement: the SKU read (~:470) gains `hazardClass`; the re-derivation attr object (:588-594) feeds it; after the locked-row class gate (:539-547), before the load read, a new exported `occupantHazardClassesInTx(tx, tenantId, warehouseId, binId)` (distinct hazard classes of OTHER skus with `quantity > 0` in the bin — the same join shape as `binOccupancyInTx` :1065-1101; the warehouse arm is the tenant-scoping defense, `binId` alone is already a uuid PK) → any incompatible pair → 400 naming both SKU codes and classes. The task derivation's facade projection (`putaway.facade.ts:281-296`) gains `hazardClass` (it feeds `candidateFitsSku` at :343).
- `src/modules/tenancy/bin.command.ts` — `mergeBin`: `onHandRows` projection (:680-692) gains `hazardClass`; after the class gate (:711-723) the hazard gate reads the TARGET bin's occupants via `occupantHazardClassesInTx` and checks each moved SKU's class (moved-vs-moved is NOT re-checked — the source bin already co-locates them; recorded). Tenancy imports the putaway helper (the `binBlocked` precedent).
- `src/modules/catalog/sku.command.ts` — `EditSkuCommand` (:85-133) gains optional-PATCH-semantics `hazardClass` (undefined = unchanged; **null = clear** — the 11.2 attribute template, and unlike `storageClass` the column is nullable so the explicit-null 400 guard is NOT added); it joins the empty-patch refusal list (:180-196). The write arm (:688) carries it. **Hazard-edit guard** (in the edit tx, behind the replay, only on a CHANGE): for every non-system bin where the SKU holds stock (tenant-wide `stock_on_hand` join, the 12-1 SKU-guard shape), read the bin's OTHER occupants' hazard classes + the open QC holds on THIS SKU (origin-bin attribution — a later release would return these units to the origin bin); any bin where the new class conflicts with an occupant → 409 `hazard-segregation-conflict` naming warehouse, bin code and the conflicting SKU. Held units already sit in the system QC bin — excluded like every staging location.
- `src/modules/catalog/catalog.dto.ts` — `PatchSkuDto` (:241) gains optional `hazardClass` (`@IsIn` via the shared validator's vocabulary; nullable — no explicit-null 400 at the controller, the 11.2 template governs).
- `src/modules/catalog/import.command.ts` — `OPTIONAL_COLUMNS` (:142 area) gains `hazard_class`; the attribute block (:1215-1243) parses it — **blank cell → null** (the column is nullable; the 12-1 blank→ambient choice was NOT NULL-specific); per-row `assertHazardClass` beside the 12-1 site; the row error maps `hazardClass` → `hazard_class` (the 12-1 replace pattern).
- `src/modules/catalog/sku.command.ts` (snapshot/replay) — `SkuSnapshot` (:46-93) gains `hazardClass: string | null` — **REQUIRED**, like its 12-1 `storageClass` twin (triage row 14: `toSnapshot` always sets it; the pre-12.2 legacy widening happens at the replay cast site, `& { hazardClass?: string | null }`, the same convention as `storageClass`); the first-run snapshot and the replay serve point (:318-335) carry it (`hazardClass: stored.hazardClass ?? null` — a pre-12.2 legacy snapshot normalizes absence to null; the byte-identical legacy-digest property holds because absent optional keys drop out of the hash). Controller response mapping spreads it.
- Tests: `test/putaway.spec.ts` (placement refusal naming both SKUs, suggestion filter via `createClassBin`, task-derivation filter) · `test/bin-admin.spec.ts` (merge hazard gate, both directions: moved vs occupants) · `test/sku-attributes.spec.ts` (SKU edit guard incl. the hold arm + the null-clears verb + a class-carrying replay, import blank→null + vocabulary row error) · CHECK probe for `skus_hazard_class_check` (the 0034/0035 23514 pattern). Bootstrap: `useSuiteDatabase` + `createApp(false)`.
- Do NOT touch: the ledger registry, `qc.command.ts` release logic (pinned indirectly by the edit guard), `receiving.facade.ts` snapshot (12-8's), any FE file, `stock.adjust`.

## Tasks & Acceptance

**Execution:**
- [x] Migration `0036` — `bun run db:generate`, then hand-append the CHECK; journal row comes from generate.
- [x] `src/shared/primitives/hazard.ts` — vocabulary, `hazardClassesCompatible`, `assertHazardClass`, the two refusal factories.
- [x] `schema.ts` — `skus.hazardClass` with doc comment.
- [x] Putaway — candidate shape (+ `occupants` pair aggregate), `candidateFitsSku` hazard arm with the `movingSkuId` skip, placement co-location query + refusal, facade projection. *(amended, review loopback 1)*
- [x] Tenancy — merge hazard gate (projection + target-occupant read).
- [x] Catalog — SKU edit field (DTO + empty-patch list + write arm) + 409 guard with the hold arm, import column, snapshot/replay normalization.
- [x] Tests per suite above; `bun run openapi:export` (additive-only — FE regen is NOT this story).
- [x] `docs/design/PENDING.md` — extend the adjustment-bypass entry to name the hazard gate.

**Acceptance Criteria:**
- Given a bin holding an oxidiser-class SKU, when a flammable SKU is placed into it, then the placement 400s `bin-segregation-conflict` naming both SKUs and classes, and nothing is written; when the suggestion runs for the flammable SKU, that bin is never suggested.
- Given the same bin, when a non-hazardous SKU is placed into it, the placement succeeds — a null class carries no segregation rule (deliberate narrowing, recorded).
- Given a merge whose source holds a hazard class incompatible with a target-bin occupant, when `mergeBin` runs, it refuses whole naming both parties, before any arm moves.
- Given a SKU edited to a hazard class incompatible with its current binmates, the edit 409s `hazard-segregation-conflict` naming the bin and the party; the same edit clearing the class (null) always succeeds.
- Given an open QC hold on the edited SKU whose origin bin holds an incompatible occupant, the edit 409s on the hold arm — a later release can never return stock to a conflicting bin.
- No existing sequential behavior changes — the full suite stays green.

</frozen-after-approval>

## Implementation Notes

## Review loopback 1 (bad_spec, rows 2-5) — re-derivation contract

**Trigger.** The code review verified that the Code Map's candidate shape — a class-only `hazardClasses: readonly string[]` aggregate over all `stock_on_hand` rows — cannot serve the decided same-SKU-consolidation rule (the placement and merge gates skip the moving SKU's own pairs; the walk cannot) and carries drained (quantity-0) residue every gate read excludes. The root cause is the non-frozen Code Map; the frozen constraints stand (the richer projection keeps the list SKU-agnostic — no WHERE arm).

**KEEP instructions (must survive re-derivation):**
- The migration 0036 shape exactly as verified: `ALTER TABLE skus ADD COLUMN hazard_class text;` + the hand-appended nullable CHECK (`IS NULL OR IN (...)`) — plus a `--> statement-breakpoint` after the ADD COLUMN (the 0035 pattern; the prior build missed it).
- `hazard.ts` verbatim as verified: `HAZARD_CLASSES` 7-tuple, `INCOMPATIBLE_PAIRS` encoded under SORTED keys, `hazardClassesCompatible` (null→true before the explosive check — explosive+null is compatible, the decided narrowing), `assertHazardClass` (skips undefined/null), the two refusal factories (400 `bin-segregation-conflict` / 409 `hazard-segregation-conflict`).
- The placement co-location gate position (after the 12-1 class gate, before the load read, inside the bin-row `.for('update')` window) with its own-`skuId` skip; the mergeBin gate (target occupants via `occupantHazardClassesInTx`, moved-vs-moved not re-checked, moved rows with a null class skip); the SKU hazard-edit guard (409, tenant-wide non-system stock + the QC-hold origin-bin arm, clear always succeeds, `.for('update')` whenever the field is present).
- The legacy-digest pattern: absent `hazardClass` drops from the payload hash; replay serves `hazardClass: stored.hazardClass ?? null`; import blank → null; the row error maps `hazardClass` → `hazard_class`.
- The test coverage the prior build achieved (placement refusal naming both parties, suggestion/task filter in the single-bin warehouse, merge both directions + moved-vs-moved + same-SKU top-up + null-class merge, edit guard incl. hold arm + clear verb + replay arms + legacy replay, import column + row error, CHECK probe) — carried into the re-derived suite.
- Every final newline on new files.

**Known-bad state to avoid:** an explosive SKU topping up its own bin — the placement gate allows it (own-sku skip), but a class-only walk refuses the bin, so the legitimate top-up 400s for a missing reason code with a null or wrong recorded suggestion; and a bin whose hazardous stock drained keeps refusing suggestions the gate admits.

## Spec Change Log

- **2026-09-24 (review loopback 1, bad_spec):** the code review found the candidate projection the Code Map specced — `hazardClasses: readonly string[]`, a class-only aggregate — cannot serve the decided same-SKU-consolidation rule, and counts drained (quantity-0) stock rows that every gate read excludes. Amended Code Map (putaway section): `PutawayBinCandidate` carries the occupants as **(skuId, hazardClass) pairs** over `quantity > 0` occupants (the list stays SKU-agnostic — no WHERE arm; the projection is richer, the rule still runs per-candidate on the SKU side), and `candidateFitsSku` gains a `movingSkuId` argument whose own pairs are skipped (the same arm the placement and merge gates carry). Known-bad state avoided: an explosive SKU topping up its own bin — the placement gate allows it (own-sku skip), but the class-only walk refuses the bin, so every legitimate top-up 400s for a missing reason code and the recorded suggestion is wrong. KEEP instructions live in the loopback section below.

## Review Triage Log

| # | Finding (lens) | Verdict | Disposition |
| - | - | - | - |
| 1 | The placement's re-derived suggestion passes `hazardClass` into the walk (`putaway.command.ts:658-664`) but no test observes the re-derived `placement.suggestedBinId`/`suggestedBinCode` in any hazard state (VG1) — the recorded suggestion can point at a bin the co-location gate refuses, undetected | **medium** — pre-verified gap (its demonstration: dropping the key at this one site passes every existing test while breaking the SYNC HAZARD invariant on this arm) | **patch** — assert the re-derived suggestion excludes an incompatible bin in a two-bin layout where the incompatible bin is the lower-occupancy candidate (the `putaway.spec.ts:706-708` assertion shape) |
| 2 | `binCandidatesInTx`'s `hazardClasses` aggregate has no `quantity > 0` arm (VG-O1): the join filters on bin/tenant/warehouse only, `addToOnHand` upserts and never deletes drained rows (`ledger.service.ts:657-684`), so a bin whose hazardous stock was fully picked keeps the SKU's class in every candidate list — the suggestion walk refuses (or mis-reasons) placements the placement gate itself admits (`occupantHazardClassesInTx` and the merge source read both filter `gt(quantity, 0)`) | **medium** — verified: join read at `putaway.command.ts:1080-1087`, no filter on the aggregate | **patch** — add `stock_on_hand.quantity > 0` to the aggregate's `filter` clause; folded into the re-derivation amendment (same aggregate line) |
| 3 | Same claim as #2 from the edge-case lens (EC1): a drained bin keeps its stale hazard class, the suggestion never proposes a bin the gate allows | **medium** — same location, same claim; verified once | **patch** — same fix as #2 |
| 4 | Same claim as #2 from the blind lens (BH3): suggestion and gate can disagree on the same bin | **medium** — same location, same claim; verified once | **patch** — same fix as #2 |
| 5 | `candidateFitsSku`'s hazard arm lacks the own-SKU exclusion the placement and merge gates carry (EC2): the candidate carries only DISTINCT classes, so an explosive SKU topping up its own bin is refused by the walk (`hazardClassesCompatible(explosive, explosive)` → false) while the placement gate allows it (own-`skuId` skip) — and the placement's mismatch gate then 400s the legitimate top-up for a missing reason code, with the recorded suggestion null or pointing elsewhere. Reachable: first explosive placement into an empty bin, then a top-up | **medium** — verified: gate's own-sku skip at `putaway.command.ts:575-578`, walk's class-only arm at `:1006-1011`, mismatch 400 at `:661-665` | **bad_spec** — the Code Map specced this exact shape (`hazardClasses: readonly string[]`); the frozen "candidate list stays SKU-agnostic" constraint is not the root cause (a richer projection is still SKU-agnostic — no WHERE arm), so the root cause sits in the non-frozen Code Map. Loopback: amend the Code Map, re-derive |
| 6 | The placement/merge gates read occupant classes without SKU-row locks; a concurrent hazard edit of an OCCUPANT can interleave (EC3) — the recorded PENDING currency (`putaway:58`) names only the INCOMING SKU's unlocked read, not the occupant half | **medium** — verified: the same unlocked-read family as 11-5/12-1's accepted currency; the occupant-side window is genuinely new with 12-2 but inherits the same shape (single-transaction pair, one misplaced placement) | **defer** — recorded currency, same accepted family; the defer entry extends the PENDING currency to name the occupant-side window |
| 7 | The decided matrix is pinned by exactly one pair (BH1): only oxidizer↔flammable is exercised; no unit test of `hazardClassesCompatible` (symmetry, null arm, unknown value); `corrosive-acid↔corrosive-base`, `corrosive-acid↔toxic`, `gas↔oxidizer` and the explosive universal rule are untested everywhere | **medium** — verified: grep over the tests finds no other pair; the human decision of 2026-09-23 rests on one exercised pair | **patch** — a unit test of the predicate (all 5 rules, symmetry, null arms, explosive↔explosive) + gate-level probes for the other pairs |
| 8 | The explosive rule and the null narrowing interact unpinned (BH2): null is checked before explosive in `hazardClassesCompatible`, so an explosive SKU co-locates with null-class stock — the decided narrowing, but no test pins it and the wms-be README's "`explosive` segregates from ALL" overclaims (null is not a class) | **low** — behavior is the recorded design; the harm is doc wording + an unpinned distinctive rule | **patch** — pin the pair in the unit test; reword the README line to "segregates from every CLASSED SKU (a null class carries no rule even beside an explosive)" |
| 9 | The suggestion rationale collapses segregation refusals into "No conforming storage bin has room for these units" (BH4) — the spec chose the wording deliberately (Code Map: "the nothing-fits rationale is unchanged") | **low** — real observability gap, deliberate spec decision | **defer** — currency for the 12-7 admin surface; recorded in deferred-work.md |
| 10 | `0036` lacks the `--> statement-breakpoint` marker between its two statements (BH5) while 0035 (the cited pattern) marks after every statement including the hand-appended CHECKs | **low** — verified (0035 read in full; functionally tolerated, convention deviation real) | **patch** — insert the marker after the ADD COLUMN |
| 11 | The hazard-edit arm's comment says the SKU row is locked "when the hazard class is moving", but the code locks whenever the field is present — a no-op edit and a clear both lock (BH6); the 12-1 arm above carries the same comment inaccuracy and 12-2 copies it | **low** — verified; over-locking is harmless (single-row lock, tiny window), comment-code mismatch is the harm | **patch** — reword both comments to "whenever the field is present" |
| 12 | The legacy-replay fixture omits `reorderPoint`/`reorderQty`, which every real snapshot has carried since 10.1 (`SkuSnapshot:105-106`) (BH7) — the fixture does not reproduce the shape it claims to, and the `toMatchObject` assertion hides the gap | **low** — verified | **patch** — add both fields to the fixture |
| 13 | The list-endpoint comment justifies `hazardClass: item.hazardClass ?? null` via "the replay serve point normalizes…" (BH8) — but the list path reads live `skus` rows through `toSnapshot` (`sku.command.ts:209-240`) and never serves replays; the stated reason does not apply | **low** — verified; the normalization itself is correct (a type normalizer) | **patch** — reword the comment to the real reason (`toSnapshot` always sets it; the normalizer pins the required response field) |
| 14 | `SkuSnapshot.hazardClass` is optional on the interface while its 12-1 twin `storageClass` is required with the legacy widening at the cast site (BH9) — two conventions for the same legacy problem, and TS no longer forces `toSnapshot` to carry the field | **low** — verified; harm is the weakened compiler pin (the controller normalizes, so no runtime harm today) | **patch** — make `hazardClass` required on `SkuSnapshot`; the legacy widening already happens at the cast site (`& { hazardClass?: string \| null }`) |
| 15 | The 12-2 hold-party message omits the hold id while 12-1's names it ("hold \"\<id\>\"", `sku.command.ts:492`) (BH10) — the 409 gives no way to find which hold pins the bin | **low** — verified | **patch** — include the pinning hold's id in the party message (the `holds` list is in hand) |
| 16 | The placement's same-SKU consolidation arm is covered only through the merge surface; no case places a hazard-classed SKU into a bin already holding its own units through `POST …/placements` (BH11) | **low** — verified | **patch** — add the top-up assertion to the placement test |
| 17 | The guard's positive arm is untested: no case shows a compatible hazard change succeeding with co-located stock present (BH12) — a regression that refused everything would pass | **low** — verified | **patch** — add a compatible-class edit with a binmate present, expecting 200 |

**Routing:** row 5 is `bad_spec` (review loopback 1 — the Code Map's candidate shape cannot serve the decided same-SKU rule, and its aggregate misses the gate-side quantity filter, rows 2-4). Rows 1, 7, 8, 10-16 are `patch` (re-applied after re-derivation). Rows 6, 9 are `defer`. Rows 2-4 fold into the re-derivation (same aggregate line as row 5).

### Round 2 (after review loopback 1)

| # | Finding (lens) | Verdict | Disposition |
| - | - | - | - |
| 18 | The device-facing 400 `bin-segregation-conflict` is absent from the placement endpoint's 400 `@ApiResponse` arm (`putaway.controller.ts:65`) and from `mergeBin`'s (`tenancy.controller.ts:520`) — `openapi.json` carries ZERO occurrences of the code (grep-verified) though both surfaces return it; the SKU PATCH's 409 arm WAS annotated in the same story (BH1, BH2, VG2 — pre-verified with the grep) | **medium** — the machine contract cannot discover the new error code on two of its three gated surfaces; the same annotation line also still omits 12-1's `bin-storage-mismatch` (pre-existing debt, same edit) | **patch** — name `bin-segregation-conflict` in both 400 arms (and `bin-storage-mismatch` beside it — the 12-1 currency, one edit either way); re-export openapi |
| 19 | Same claim as #18 from the edge/verification lenses (VG2 carries the grep evidence) | **medium** — same location, same claim | **patch** — same fix as #18 |
| 20 | The no-op same-class hazard edit skips the guard body (`hazardLocked.hazardClass !== newHazardClass`, `sku.command.ts:532`) but no test re-submits a SKU's CURRENT class over now-segregated stock (VG1-r2) — a dropped change-check would 409 a legitimate re-class retry, undetected; the demonstration is the reachable null-then-class two-SKU premise | **medium** — pre-verified gap (its demonstration: dropping the conjunct passes every existing test while falsely refusing re-submissions) | **patch** — after the guard test's 409, re-patch the same class expecting 200 |
| 21 | The hazard-edit guard issues one `occupantHazardClassesInTx` query per stocked bin and per hold origin inside the `.for('update')` window (BH7-r2, `sku.command.ts:546-560, 588-596`) — a per-bin N+1 under a row lock, regressing the one-grouped-query shape the prior build verified (and the 12-1 guard's own shape) | **medium** — verified; rare command, small N, but the shape diverges from the Design Notes' one-grouped-query claim | **patch** — one grouped occupant read over `stock ∪ hold-origin` bin ids, the same shape the placement and merge gates use |
| 22 | When a bin is both a stock bin and a QC-hold origin, the same binmate is pushed into `parties` twice — once per stock scan, once per hold scan with the hold id (BH8-r2, EC1-r2) — the 409 detail lists the identical conflict twice with no dedupe on (binId, skuId) | **medium** — verified in the guard body read above; user-facing duplicated refusal detail | **patch** — grouped with #21: dedupe parties on (binId, skuId), the hold-attributed copy winning the richer message |
| 23 | Same claim as #22 from the edge lens (two open holds sharing one origin bin repeat the party per hold) (EC1) | **medium** — same location, same claim | **patch** — same fix as #21/#22 |
| 24 | The 409 tells the operator to "relocate the stock first", but no gated bin-to-bin relocation surface exists — only the (same-rule-gated) merge or the bypassing `stock.adjust` (BH3-r2) | **low** — the wording is inherited verbatim from 12-1's accepted 409; the gap is the missing transfer surface itself (Epic 5, re-sequenced after Epic 12), not this story's change | **defer** — Epic 5 material; recorded in deferred-work.md |
| 25 | The `stock.adjust` bypass is annotated nowhere at its own site (`inventory.command.ts`) (BH4-r2) — the bypass is named only in `hazard.ts`'s docstring and PENDING | **low** — verified | **patch** — one comment line at the adjust command naming both bypasses (capacity, 11-5; storage + hazard class, 12-1/12-2) |
| 26 | The CHECK probe rejects only `'biohazard'`; nothing inserts each of the 7 vocabulary values directly, so widening one layer without the other passes CI (BH5-r2) | **low** — verified; the hand-mirrored CHECK tuple is otherwise unpinned against `HAZARD_CLASSES` | **patch** — extend the probe to insert each of the seven values once |
| 27 | Unknown-value fail-open lives only in the test comment (BH6-r2) — unknowns cannot reach the predicate (every write edge is closed by `assertHazardClass`/`@IsIn`/CHECK) and the docstring documents null and explosive but not the unknown-value fall-through | **low** — verified; one docstring sentence fixes the contract's blind spot | **patch** — one docstring line: a class outside the pair set is compatible with every classed SKU; vocabulary widening happens HERE, pairs included |
| 28 | The import row error's `replace('hazardClass', 'hazard_class')` is fragile / the fallback drops the vocabulary list (BH9-r2) | **false** — `assertHazardClass`'s detail names the field exactly once (`hazardClass must be one of … (got "…")`), so a first-occurrence replace is correct; the fallback is unreachable defensive code (detail is always a string) | Rejected |
| 29 | The 400/409 code split for merge vs SKU edit is unjustified (BH10-r2) | **false** — the Code Map prescribes exactly this split ("the 12-1 split (device-facing refusal vs state conflict), deliberate"), and 12-1's review rejected the identical claim at its triage row 10 for the same reason | Rejected |
| 30 | `binSegregationConflict`'s `incomingClass ?? 'no hazard class'` arm is dead code (BH11-r2) | **false** — the re-derived factory takes a prebuilt detail string (`hazard.ts:122`); no such arm exists | Rejected |
| 31 | No test patches `storageClass` AND `hazardClass` in one body — the two guard arms' interaction (one passing, one 409ing) is untested (BH12-r2) | **low** — verified | **patch** — add the combined-body case to the edit-guard test |
| 32 | The merge gate's moved-vs-moved premise ("the source already co-locates them") depends on `stock.adjust` being the only writer that can form incompatible co-location — the coupling is not named at the gate (BH13-r2) | **low** — PENDING's bypass entry records the coupling, but the gate's own comment states an unconditional premise | **patch** — one comment line naming the premise's condition |
| 33 | `0036_hazard_class.sql`, `hazard.ts` and `hazard.spec.ts` end without a trailing newline (BH14-r2, EC4-r2) — the re-derivation's KEEP instruction required final newlines | **low** — verified by byte read | **patch** — append the newlines |
| 34 | The Code Map's `SkuSnapshot` line still says `hazardClass?: string | null` (optional) while triage row 14 decided required, and the `occupantHazardClassesInTx` sketch says a 3-arg signature while the implementation rightly scopes by warehouse too (EC2-r2, EC3-r2) | **low** — the code is right; the Code Map text is stale against the triaged shape | **patch** — Code Map sync folded into this spec's step-05 commit (recording the triaged shape, not a new decision) |

**Round-2 routing:** rows 18-26, 27, 31-33 are `patch` (one message to the implementer: annotation arms + guard-body grouped/dedupe rewrite + the test/comment/newline set). Row 24 is `defer`. Row 34 rides the step-05 spec commit. Rows 28-30 rejected.

## Design Notes

- **Two rules, one shape.** 12-1's rule is (SKU, bin): static, encoded in a rank table. 12-2's rule is (SKU, SKU-in-bin): dynamic, encoded as a symmetric pair set. Both live in TS primitives, both are imported by the same gated-writer set, both refusal families split 400-device-facing from 409-state-conflict. A placement now runs: structural arms → storage-class gate → **hazard co-location gate** → capacity gates.
- **The co-location read is one grouped query** (`occupantHazardClassesInTx`): `select distinct skus.hazard_class from stock_on_hand join skus where bin_id = ? and quantity > 0 and hazard_class is not null and sku_id <> incoming` — inside the bin-row `.for('update')` window, so a concurrent placement cannot slip an incompatible unit between the scan and the write (the 12-1 edit-guard serialization argument).
- **`binCandidatesInTx` gains one aggregate, not a query.** The candidate list stays one grouped query (the SKU-agnostic rule); the occupants' (skuId, hazardClass) pairs ride the existing stock_on_hand→skus join — `quantity > 0` and non-null class in the aggregate filter (amended, review loopback 1: a class-only aggregate over all rows cannot skip the moving SKU's own pairs and would carry drained residue). Bins with no hazardous occupants carry an empty array — the walk's common case pays one aggregate, not a N+1.
- **Default matrix is safe by construction:** no pre-existing state can become conflicting because no hazard class existed before; every existing SKU reads null — rule-free. No backfill.
- **Null carries no rule, in both directions:** a non-hazardous SKU may share a bin with hazardous stock (the matrix rules pairs of classes), and hazardous stock never refuses a placement by its own presence alone. This is the recorded FR-41 narrowing — FR-41 refuses *incompatible pairs*, not hazard-presence.
- **Residual races, recorded in PENDING by this story:** (a) `stock.adjust` bypasses the co-location gate (the 12-1 class-gap precedent, same entry); (b) the placement reads the incoming SKU's hazard class unlocked (the 12-1 unlocked-SKU-read currency covers the same window).
- **Legacy digests:** `EditSkuCommand.hazardClass` absent drops out of the payload hash (`JSON.stringify` drops undefined), so a pre-12.2 key replays byte-identically; a stored legacy snapshot serves `hazardClass: null` on replay (the `storageClass ?? 'ambient'` pattern's nullable twin).

## Verification

**Commands:**
- `cd workspace/core/backend/wms-be && bun run lint && bun run typecheck` — exit 0.
- `bunx jest test/putaway.spec.ts test/bin-admin.spec.ts test/sku-attributes.spec.ts` — all green.
- `bun run test` — full suite green, fresh-isolated (one run at a time: two concurrent jest processes race on the template DB); **this is also the migration proof** (global-setup applies `0036` to `wms_template`).
- Migration CHECK proof against `wms_template`: `skus.hazard_class` nullable + `skus_hazard_class_check` present in `information_schema.table_constraints`; a live insert of `hazard_class = 'biohazard'` expects 23514; a live insert of `null` succeeds (nullable arm).
- `bun run openapi:export && git diff --stat openapi/openapi.json` — additive-only (new nullable `hazardClass` on `SkuResponse`/`PatchSkuDto`).