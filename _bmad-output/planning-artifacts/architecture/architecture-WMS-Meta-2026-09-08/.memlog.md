---
scope: WMS v1 — full platform spine (web dashboard + mobile scan client + backend)
purpose: build substrate for wms-be / wms-fe builds, companion to prd-WMS-Meta-2026-09-07
altitude: initiative
updated: 2026-09-08T00:11
---

- (direction) Input: prd-WMS-Meta-2026-09-07 (PRD + addendum + memlog). Fast path, spine only. Child repos wms-fe/wms-be are empty skeletons — greenfield build, no existing conventions to ratify.
- (decision) [ADOPTED, addendum §1.4] Paradigm: modular monolith with clean event seams; ledger-event-sourced inventory core. No microservices in v1. Prevents microservice sprawl and shared-database coupling between features.
- (decision) [ADOPTED, addendum §1.1] Append-only Inventory Ledger + derived projections (on-hand, ATP, KPIs); snapshots/checkpoints avoid full replay. No code path mutates quantity without a ledger event. Prevents unauditable mutable-quantity drift (FR-4, NFR-1).
- (decision) [ADOPTED, addendum §1.1] Atomic reservation: relational DB kept out of the check-and-decrement hot path; in-memory/single-writer ATP state with sub-10ms decisions, event stream to persistence. Prevents row-lock queuing at flash-sale load (FR-5, NFR-2).
- (decision) [ADOPTED, addendum §1.1] Idempotency key on every write — non-negotiable given offline mobile queueing (NFR-3) and channel webhooks. Prevents duplicate ledger events from replays.
- (decision) [ADOPTED, addendum §1.2] Tenancy: shared schema, tenant_id on every table and access path; config-as-metadata (per-tenant workflows in metadata tables, never code); warehouse_id as first-class partition key from day one. Prevents cross-tenant leakage (NFR-4) and a warehouse-count retrofit.
- (decision) [ADOPTED, addendum §1.3] Offline mobile sync: local store + outbox FIFO + ULID idempotency keys; replay on reconnect in task order; LWW conflicts escalate to human review, never silent resolution; scan decision logic runs on-device. Prevents scan loss in dead zones (FR-14, NFR-3).
- (decision) [ADOPTED, addendum §1.4] Deferred architecture-level: multi-region pinning, CRDTs/vector clocks, 2PC across services, RFID edge services, per-tenant databases, noisy-neighbor isolation beyond load-shedding priority.
- (constraint) PRD NFRs bind verbatim: NFR-1 zero ledger/derived divergence (continuous replay-reconciliation), NFR-2 15x median order rate 2h no oversell, NFR-3 offline ≥30min zero scan loss idempotent replay, NFR-4 tenant_id on every access path, NFR-5 7-year audit retention, NFR-6 latency budgets (web p95<2s, scan ≤1.5s on-device, sync p95≤60s, label gen p95≤5s).
- (assumption) India-region hosting preferred for GST-adjacent data (PRD Open Question 6 unresolved — spine picks default region, tags it).
- (question) Open from PRD: carrier set/adapters (OQ1), marketplace API approval timing (OQ2), e-way bill path portal-API vs GSP (OQ3), serial tracking v1 vs v1.5 (OQ5) — spine treats these as pluggable seams, not blockers.
- (decision) [ADOPTED, user pick from researched alternatives 2026-09-08] Stack: TypeScript everywhere — NestJS 12 backend (Node 22 LTS), Next.js 16 frontend, Expo 57/React Native 0.86 mobile. Why TS over Python/Go: Bun-only workspace already leans JS/TS; one language across 3 surfaces; interface contract becomes generated types instead of prose. Alternatives rejected: FastAPI backend (two languages, OpenAPI codegen overhead), Go backend (fastest runtime, slowest agent iteration; hot path is language-agnostic anyway).
- (decision) [ADOPTED, user pick] Reservation hot state lives in Valkey (Redis-compatible, BSD-3, Linux Foundation): Lua check-and-decrement, atomic sub-ms; ATP state rebuilt from Postgres ledger on cold start. Alternatives rejected: in-process single writer (caps horizontal scale), Postgres-only (contradicts addendum §1.1 keep-DB-out-of-hot-path).
- (version) Verified on web 2026-09-08: NestJS 12.0.0 (2026-08-27, Bun supported, Standard Schema/Zod), Next.js 16.3.4, Expo SDK 57 / RN 0.86 (2026-06-30, Node 22.13+), PostgreSQL 18.6 (uuidv7 native, virtual generated columns), Valkey 9.1 / Redis 8.10 (tri-license RSALv2/SSPLv1/AGPLv3 — Valkey chosen for BSD-3).
- (decision) AD-5: every mutating API/webhook carries Idempotency-Key (ULID); backend stores key→response and de-dupes before writing events; integration consumers idempotent — at-least-once delivery, exactly-once effect. Prevents duplicate ledger events from webhook retries/double-tap/offline replay.
- (decision) AD-6: monolith modules own their tables privately; cross-module access only via injected interfaces and domain-event subscriptions; inventory module depends on nothing and may never import another module. Prevents distributed-monolith schema coupling.
- (decision) AD-7: transactional outbox — state change + outbound event in one Postgres transaction, relay publishes at-least-once; external failures (carrier API, e-way portal) retry with backoff and never block/roll back domain state (dispatch records first).
- (decision) AD-8: wms-be exposes versioned OpenAPI as the single API truth; wms-fe/wms-mobile commit generated typed clients; no hand-written API types; type breakage is a build failure in the consuming repo. Keeps docs/repos interface contracts machine-checkable.
- (decision) AD-9: quantities integer base-UoM, money integer paise, GST basis points, timestamps ISO-8601 UTC (IST display-only), IDs UUIDv7 (client-generatable, time-ordered), event qty signed by direction.
- (decision) AD-10: one command layer owns all state mutation (user action, scheduler, integration); role/approval gates live there; clients thin — on-device scan validation mirrors, never overrides, server rules.
- (assumption) wms-mobile lives in its own repo (not inside wms-fe): independent build/store pipelines; requires meta-repo follow-up — docs/repo-catalog.yaml entry, workspace registration, docs/repos/wms-mobile/ contract.
- (assumption) Infra: AWS ap-south-1 Mumbai (ECS Fargate, RDS Postgres, ElastiCache Valkey, S3); PRD OQ6 only states India-region preference — provider choice tagged, correctable in review.
- (version) Drizzle ORM pinned 0.45.x (stable); 1.0 series still RC as of 2026-09-08 — deferred until stable. Zod 4.x; TanStack Query 5.x; expo-sqlite SDK 57.
- (event) Reviewer gate: lint 0 findings; 6 parallel reviewers (rubric, currency, adversarial, security, integrity, PRD-reconcile). Verdicts: not adoptable as-is / conditional adopt across the board — convergent criticals on reservation durability, buffer math, offline replay, ledger schema registry, secrets, audit tamper-evidence. Full reviews in reviews/.
- (decision) AD-2 tightened (gate): reservation decisions still atomic in Valkey Lua (pre-declared keys, hash-tag co-location) but Postgres is truth — winning grant journaled as durable reservation record in the state-change tx; cold start rebuilds Valkey from ledger+journal; reconciliation reverses unjournalled Valkey state (Postgres wins); durable event commits before Valkey mirror applies. Fixes IN-01/C-2/C-5 (split-brain, ordering).
- (decision) AD-12 new: reservations are durable first-class records (owner, TTL, state held→committed→released/expired) with a reaper; release paths serialize through reservation identity — exactly one terminal transition. Fixes ATP leakage from accepted-never-dispatched orders (IN-03).
- (decision) AD-13 new: channel Safety Buffers are standing reservations through the AD-2 script, owned by channels module; inventory publishes only unallocated sellable (on-hand − QC-held − committed) and knows nothing of channels. Fixes C-3 (sync-time buffer math double-sells at flash-sale load; also respects AD-6 direction).
- (decision) AD-14 new: offline client settles only pre-reserved task stock (waves reserve at release); new ATP grants always online; per-bin state_epoch + 4-case conflict taxonomy; bin on-hand never negative. Fixes IN-02/C-4 (offline bypasses reservation, concurrent-mutation races).
- (decision) AD-11 new: ledger event envelope is a versioned registered schema (schema_version, event-type enum, typed reference_doc union, batch/serial union arms) + server-assigned gap-free per-warehouse sequence as replay order (UUIDv7 not a total order). Fixes C-1 (two builders, incompatible event grammars) and IN-10.
- (decision) AD-15 new: third-party credentials (marketplace OAuth, carrier keys, GSP) per-tenant KMS envelope encryption, referenced by id, rotation first-class, delete+revoke on disconnect, never in logs/exports/ledger. Fixes security CRITICAL (no secrets invariant).
- (decision) AD-16 new: ledger hash-chained, head periodically WORM-anchored; exports carry digest; retention ≥7yr as implemented lifecycle (partitioning, archive tiering, restore-verified); chain break = severity-1 alert. Fixes security CRITICAL (NFR-5 a number with no architecture) + reconciler miss #6.
- (decision) AD-17 new: background work is sheddable — scan path > background sync under load (NFR-4 priority half, reconciler miss #1); per-tenant integration call metering with automatic circuit breaking (sync storms; also the monetization metering surface — reconciler miss #2).
- (decision) Tightenings from gate: AD-3 scoped to background jobs/projections/exports/Valkey key namespacing (superuser bypass = audited exception); AD-4 encrypted store + device enrollment/revocation + replay re-authorization against current role/device; AD-5 webhook idempotency derived from (integration, event-id, verified payload hash), tenant-scoped, windowed retention, signature verification precondition; AD-6 exclusive entity ownership (tenancy=bin master, putaway=bin ops, catalog=batch/serial identity, ledger=quantities, outbound=order state machine); AD-7 DLQ + sync-lag SLO; AD-10 role epoch re-evaluated at command entry (FR-3), JWT is transport not authority; AD-9 regulatory constants as versioned config data.
- (version) Stack corrections from currency review: Node 24 LTS (22 is maintenance), TypeScript 6.x (7.x GA but programmatic API lands 7.1), Drizzle ≥0.45.2 (CVE fix), codegen toolchain pinned openapi-typescript 7.13 + @hey-api/openapi-ts 0.99.x; Valkey 9.1 ElastiCache GA with pre-declared-keys Lua constraint. All other pins confirmed current (NestJS 12, Next 16.3.4, Expo 57/RN 0.86, PG 18.6 on RDS, Zod 4).
- (direction) Operational envelope de-lumped after rubric finding: observability/alerting surface load-bearing for NFR-1 (first-class, tooling deferred); load testing 15× pre-peak scheduled; DR restore-verified; IaC/CI-CD/on-call tooling deferred.
- (event) spine finalized (17 ADs, lint clean, reviewer gate resolved). Remaining [ASSUMPTION]s non-blocking: infra provider (AWS tagged), wms-mobile repo registration (follow-up meta-repo change), mobile scanning internals (feature-level).
