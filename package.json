{
	"name": "wms-meta",
	"private": true,
	"description": "Meta repo coordinating the WMS frontend (WMS-FE) and backend (WMS-BE) repos for AI-assisted, cross-repo development.",
	"engines": {
		"node": ">=18",
		"bun": ">=1.3"
	},
	"packageManager": "bun@1.3.13",
	"scripts": {
		"preinstall": "node -e \"if (!/bun\\//.test(process.env.npm_config_user_agent||'')) { console.error('\\nThis repo uses bun only. Run: bun install\\n'); process.exit(1); }\"",
		"prepare": "git config core.hooksPath hooks",
		"workspace:setup": "bun scripts/setup-workspace.ts",
		"workspace:status": "bun scripts/workspace-status.ts",
		"workspace:push": "bun scripts/workspace-push.ts",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"ink": "^7.1.1",
		"react": "^19.2.8",
		"yaml": "^2.8.1"
	},
	"devDependencies": {
		"@types/bun": "^1.3.13",
		"@types/react": "^19.2.18",
		"typescript": "^5.8.0"
	}
}